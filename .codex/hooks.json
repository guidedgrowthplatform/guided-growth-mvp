{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if jq -r '.tool_input.command // \"\"' | grep -q '^git commit'; then echo 'BLOCKED: Do not commit without explicit user permission.' && exit 2; fi",
            "timeout": 5
          }
        ]
      }
    ]
  }
}

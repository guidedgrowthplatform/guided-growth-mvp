export const RESUME_CONTINUATION_RULE = `
## Resuming
The messages above are real earlier conversation, not examples. Continue naturally from where you left off. Do NOT greet again, reintroduce yourself, or re-ask anything already answered. If unsure where things stand, ask one brief question to pick up the thread — never restart from the beginning.`;

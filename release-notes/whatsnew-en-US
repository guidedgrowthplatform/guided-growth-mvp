v2.8.3 — Onboarding chat reliably advances through every step; fixes for app-loading errors after an update (Android + iOS).
Test: 1) Run onboarding; each chat reply should move you to the next step. 2) Reopen the app after updating — no blank screen or load error. 3) Browse around on Android; pages should load without import errors.

# MR !531 and !514 Status Check

## MR !531 — Bible fill (beat 13 / goals-sleep)

**Status:** Draft, no conflicts. Source: `899c6774`.
**Files changed:** Only `beatsSource.ts` (formatting + beat 13 context additions).
**Tile-preselect render fix:** NOT IN THIS MR. The MR touches only prose/context, not component render code.
**Ready to merge:** No—category-grid pre-select tile render bug remains outstanding.

## MR !514 — Source-integrity guards (draft)

**Status:** Draft, no conflicts. Source: `dbe6143e`. Target branch: `flow-annotated-render` (not main—**stale target**).
**Updated:** 2026-07-08 20:20 (no movement in 1+ day).

**Guard families implemented:**
- ✅ A. Fallback flow source (audit #4)
- ✅ B. Generated-file consumption (audit #5)
- ✅ C. Legacy screen_contexts (audit #6)
- ✅ D. Orphan audio / live-voice breach (audio-ownership-check) (audit #8)
- ✅ E. Prose-only behavior rules (audit #9)

**Missing families:** check:render, check:links, id-alias-check, persistence-contract-check, decisions-coverage-check.

**CI wiring:** YES—adds `source_integrity` job to `.github/workflows/ci.yml` running `npm run check:beats`. Currently guard is NOT required (report mode only, depends on integration flow).

**Branch currency:** Stale. Target is `flow-annotated-render` (feature branch commit `6fbd9fdd`); main has progressed to `d7b237c3`. MR can't merge to main until rebased + retargeted.

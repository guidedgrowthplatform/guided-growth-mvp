# Verification: !523 and !525 (guidedgrowth-group/guided-growth-mvp)

Read-only check via GitLab REST API v4 against `https://gitlab.guidedgrowthapp.com`.
Both target branch: `flow-annotated-render`.

## !523 — "Draft: render: comprehensive variant-beat fix (collapse + switcher + reveal gate + profile split)"

- **Draft status:** `draft: true`, `work_in_progress: true` — this MR is a GitLab Draft.
- **Source branch:** `render/collapse-variants`
- **Head SHA:** `8b3fef9ce66d8af49b1ceb54d39f310b0e3cfe0d` (short: `8b3fef9c`)
- **merge_status:** `can_be_merged`
- **detailed_merge_status:** `draft_status` (i.e., the ONLY thing blocking a real merge per GitLab's own status is the draft flag itself — no conflicts)
- **has_conflicts:** false
- **Pipelines for this MR:** `GET /merge_requests/523/pipelines` → `[]` (empty — no pipeline has ever run against this MR)
- **Pipelines for the branch directly:** `GET /pipelines?ref=render/collapse-variants` → `[]` (empty — no pipeline has ever run on this branch at all)
- **Commit statuses for head SHA:** `GET /repository/commits/8b3fef9c.../statuses` → `[]` (no job-level statuses of any kind)
- **Conclusion:** no CI has ever executed for this MR/branch. Nothing to check at job level because there is no pipeline.
- **Discussions:** 8 discussions, all non-resolvable (system notes, not open review threads). `blocking_discussions_resolved: true`. No blocking threads.
- **user_notes_count:** 0

## !525 — "Draft: fix(onboarding): sync coach-greeting audio timing, align live Cartesia voice"

- **Draft status:** `draft: true`, `work_in_progress: true` — this MR is a GitLab Draft.
- **Source branch:** `fix/render-voice-beats-3-6`
- **Head SHA:** `d11ca58decae0f234ab7b328661edf35d0e752cd` (short: `d11ca58d`)
- **merge_status:** `can_be_merged`
- **detailed_merge_status:** `draft_status`
- **has_conflicts:** false
- **Pipelines for this MR:** `GET /merge_requests/525/pipelines` → `[]` (empty)
- **Pipelines for the branch directly:** `GET /pipelines?ref=fix/render-voice-beats-3-6` → `[]` (empty)
- **Commit statuses for head SHA:** `GET /repository/commits/d11ca58d.../statuses` → `[]`
- **Conclusion:** no CI has ever executed for this MR/branch.
- **Discussions:** 0 discussions total. No blocking threads.
- **user_notes_count:** 0

## Summary

| MR | Draft? | Pipeline run? | Job failures | Blocking discussions | GO/NO-GO |
|---|---|---|---|---|---|
| !523 | Yes (WIP/Draft) | None (no pipeline exists at all) | N/A — nothing ran | None | NO-GO |
| !525 | Yes (WIP/Draft) | None (no pipeline exists at all) | N/A — nothing ran | None | NO-GO |

Both MRs are structurally mergeable (no conflicts, no blocking discussions) but (a) both still carry the GitLab Draft flag, which itself blocks a normal merge action, and (b) neither has ever had CI run against it — GitLab's pipeline/job APIs return empty for both the MR and the raw branch. There is no green (or red) pipeline to point to; verification of behavior has not happened via CI at all for either branch.

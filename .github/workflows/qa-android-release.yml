name: QA Android Build (Firebase)

# QA app (app.guidedgrowth.staging) → Firebase. Dispatch-only, decoupled from
# the prod v* tag in ci.yml. Uses FIREBASE_APP_ID_QA + the shared keystore/VITE secrets.

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Marketing version (e.g. 2.9.2). Defaults to qa-<run_number>.'
        type: string
        required: false
        default: ''

jobs:
  build-qa:
    name: Build QA APK → Firebase
    runs-on: ubuntu-latest
    # job-level so the Firebase step's `if` can read it (step env is out of scope there)
    env:
      FIREBASE_APP_ID_QA: ${{ secrets.FIREBASE_APP_ID_QA }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      # --ignore-scripts skips sharp's native binary; @capacitor/assets needs it
      - name: Build sharp native binary
        run: npm rebuild sharp

      - name: Build web
        env:
          VITE_API_URL: 'https://guided-growth-mvp.vercel.app'
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_VAPI_PUBLIC_KEY: ${{ secrets.VITE_VAPI_PUBLIC_KEY }}
          VITE_VAPI_ASSISTANT_ID: ${{ secrets.VITE_VAPI_ASSISTANT_ID }}
          VITE_CARTESIA_AGENT_ID: ${{ secrets.VITE_CARTESIA_AGENT_ID }}
          VITE_STATE3_ENABLED: ${{ secrets.VITE_STATE3_ENABLED }}
          VITE_POSTHOG_KEY: ${{ secrets.VITE_POSTHOG_KEY }}
          VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN }}
        run: npm run build

      - name: Add Android platform and copy web assets
        run: |
          [ -d android ] || npx cap add android
          npx cap copy android
          npx cap sync android
          node scripts/select-icon-master.mjs qa
          npx @capacitor/assets generate --android --assetPath assets

      - name: Decode Keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          printf '%s' "$KEYSTORE_BASE64" | base64 -d > android/app/guidedgrowth.keystore

      - name: Build QA APK
        working-directory: android
        env:
          KEYSTORE_FILE: 'guidedgrowth.keystore'
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEYSTORE_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
          # via env so a dispatch version string can't break shell quoting
          VERSION_NAME: ${{ inputs.version != '' && inputs.version || format('qa-{0}', github.run_number) }}
        run: |
          chmod +x gradlew
          ./gradlew assembleQaRelease --no-daemon \
            -PversionCode=${{ github.run_number }} \
            -PversionName="$VERSION_NAME"

      - name: Upload QA APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: guided-growth-qa-apk
          path: android/app/build/outputs/apk/qa/release/app-qa-release.apk
          retention-days: 14

      # skips silently when FIREBASE_APP_ID_QA unset
      - name: Distribute QA to Firebase App Distribution
        if: env.FIREBASE_APP_ID_QA != ''
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID_QA }}
          serviceCredentialsFileContent: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
          groups: internal-testers
          file: android/app/build/outputs/apk/qa/release/app-qa-release.apk
          releaseNotes: ${{ inputs.version != '' && inputs.version || format('qa-{0}', github.run_number) }}

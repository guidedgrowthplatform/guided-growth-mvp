> **GITLAB HOME: gitlab.guidedgrowthapp.com (migrated 2026-07-04; gitlab.com archived/read-only — do not push there).**
> **TRUNK CUTOVER 2026-07-04 (~16:00): the `staging` branch was merged into `main` and DELETED.**
> `main` is now the only trunk. Everywhere the plans and standing rules say "branch off / merge to
> CURRENT origin/staging", read **origin/main**. Open MRs were retargeted (!425 !426 by the conductor;
> !411 !415 auto). The CI gg-qa-iota alias rule was fixed to key on main (!429). No production deploy
> occurred: this CI has no prod job; Yair to confirm the Vercel project's production-branch setting.

# Onboarding bugfix status — plan of 2026-07-02

Source ledger: `gg-spec/docs/onboarding-bugfix-plan-2026-07-02.md`. One row per bug ID.
Statuses: `open` / `in-progress` / `solved` / `blocked` / `merged-into-other`.
Updated the moment a status changes; this branch (`bugfix-status-2026-07-02`) is the single source of truth.

| ID | Bug | Loop | Status | MR | Notes |
|---|---|---|---|---|---|
| B1 | QA control: "Full onboarding" didn't start from beginning | 6 | partially solved | — | c8f85490 one-tap-fresh verified live 2026-07-02; remainder = QA screen audit (Loop 6) + downstream B17/B9 |
| B2 | Coach voice toggle OFF after refresh; must start ON | 2 | solved — preview-verified 2026-07-02 | [!398](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/398) | verified on gg-gxx5woha8: voiceMode='voice' after every refresh (state-check/morning/reflection x4) + toggle leftActive in screenshots |
| B3 | Cartesia opener silent while captions render | 1 | solved — preview-verified 2026-07-02 | [!397](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/397) | verified signed-in on gg-5h6ohbzcn: POST /api/cartesia-tts 200 x4 across runs, blob plays to 'ended'; under blocked autoplay it defers to next tap then plays (no settle) |
| B4 | MP3 clips don't play; dead air + long LLM think after path answer | 1 | solved (playback facet), rework preview-verified 2026-07-02 signed OUT | [!397](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/397) | Root cause CONFIRMED as verified (double activation; stale AbortError settled the live activation on the shared pooled element). Fix 36963b50: per-activation settle tokens (openerActivation.ts, pure + 18 unit tests incl. a StrictMode hook race test), AbortError on the live activation re-arms play() (bounded) instead of settling, pool handout serialized (claimPreloadedClip/release), BeatView pins karaoke to 0 while audio is armed-not-started so say-only beats cannot silently advance (12s reveal safety kept). Verified on gg-bplca6yel (deployed preview, signed out): allowed walk = 6 MP3 beats each PLAYING before the next armed, none ended without playing; blocked run = why-intro HOLDS hands-off (8s, no playing/ended/advance), trusted tap plays to end, advances. Evidence /tmp/gg-verify/loop1-fix-*-deployed.json. Post-fork dead-air facet stays Loop 2/3 scope (see 2026-07-02 20:45 worklog). |
| B5 | Age + gender prompts merged into one bubble | 4 | fix-implemented, preview recheck pending | [!400](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/400) | |
| B6 | Chat bubbles disappear randomly | 4 | fix-implemented, preview recheck pending | [!400](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/400) | |
| B7 | Profile card disappears after age + gender given | 4 | fix-implemented, preview recheck pending | [!400](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/400) | |
| B8 | "You're signed in" banner shows during onboarding | 2 | solved — preview-verified 2026-07-02 | [!398](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/398) | banner absent in every probe across 3 loop2 runs (present on loop1 preview as expected, fix is !398) |
| B9 | Refresh mid-onboarding lands on wrong component + injects extras | 2 | solved — preview-verified 2026-07-02 (refresh matrix) | [!398](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/398) | verified on gg-gxx5woha8: refresh at state-check→state-check, morning-setup→morning-setup, reflection-setup→reflection-setup; no extra components (screenshots); evidence walk also correctly rewinds to profile when age is missing. Contrast loop1: same refresh lands on profile via numeric resume |
| B10 | Flow jumped welcome → profile, skipping in-between beat | 2 | solved — preview-verified 2026-07-02 late (resume facet + live fork jump-to-end both) | [!398](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/398) | live jump-to-end root-caused + fixed (a1a1178e): the reflection save's step-8 climb (fire-and-forget, GREATEST pin) lands AFTER the machine enters the branch node; the leading-edge advance replayed the fork's server capture (no path) and flowMachine's merge fallthrough carried it past the unanswered fork to into-app + weekly. Fix = captureCompletesBeat gate on the live advance (kept identical to !400's B21 gate) + applyCapture hard stop at an unanswered branch (merge fallthrough now opt-in for QA fastForward only). Verified signed-in on gg-5cbfl0uc5: 12s hands-off hold at the fork after a full run to reflection, server path stays null, zero into-app/weekly markers; refresh at 6/7/8 still lands the same beat; 18/18 checks (FORKFIX-* in /tmp/gg-verify/, walker fork-verify.mjs) |
| B11 | LLM calls fail repeatedly; flow wedged at habit render | 3 | solved — live A/B verified 2026-07-02 | [!401](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/401) | 3 code-verified root causes in the stream reader: unhandled `response.incomplete` (silent empty finish), `response.failed`/`error` parsed with wrong shape (all upstream errors collapsed to "Unknown error"), 600-token output cap too small for tool-heavy onboarding turns. Deterministic live repro: brain-dump turn fails 3/3 on pre-fix preview (upstream stream_error, zero output), succeeds 3/3 on the !401 preview. Plus `[llm:fail]` logging + `x-gg-debug: 1` echo, 30s client idle watchdog, one silent auto-retry then friendly bubble, opener fallback to authored line + card. Report: docs/fix-reports/b11.md on the branch |
| B12 | LLM invents process talk ("confirm your path choice") | 3 | covered by Loop 2 + existing rules — no new change | [!401](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/401) | verified against bugfix-loop2-resume's addendum rewrite (stale step ladder = the "confirm your path choice" root); NO_INTERNAL_NARRATION already bans "I'll save that for you"; prompt-source grep clean. Loop 3's client fix also stops raw error codes rendering as coach text. Report: docs/fix-reports/b12.md |
| B13 | (folded into B4) | — | merged-into-other | — | |
| B14 | First spoken word cut off at clip start | 1 | unblocked by the B4 rework; gate exercised on preview, audible spot-check pending | [!397](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/397) | first play gated on canplaythrough (2.5s bound), unchanged by the rework; with clips now actually playing (see B4) the gate runs on every warm pooled element and playback starts from currentTime 0. Automated proxy green on gg-bplca6yel; a human audible first-word listen is the remaining confirmation. USER-first-word STT variant lives on feat/onboarding-voice-track1 → Loop 5 |
| B15 | Clips not pre-buffered; start rides the network | 1 | solved — preview-verified 2026-07-02 | [!397](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/397) | verified signed-in on gg-5h6ohbzcn: all 18 clips fetch 206 within ~150ms of flow mount, both routes, autoplay allowed AND blocked |
| B16 | Mic not armed early / not opened at clip end | 5 | open | — | needs Loops 1+2 |
| B17 | QA "Restart fresh" renders old chat thread (client cache not cleared) | 4 | fix-implemented, preview recheck pending | [!400](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/400) | root cause verified: QAControlScreen restart path never clears thread store |
| B18 | Recurring staging build error re-merged by diverged branches | — | tracked-elsewhere | — | handled separately; Loop 3 rules it out before B11. **LEAD (found 2026-07-02):** origin/staging has a COMMITTED `node_modules` symlink (mode 120000) → `/Users/yairamsel/Developer/ggmvp-unified/node_modules` — a machine-local absolute path, dangling everywhere else, and exactly the kind of artifact diverged branches keep re-merging. Whoever owns B18 should delete it on staging and add a CI guard (`git ls-files node_modules` must be empty). |
| B19 | Loading bubble renders/sticks on some beats when loading transcript/components | 4 | fix-implemented, preview recheck pending | [!400](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/400) | live QA walkthrough 2026-07-02 pm |
| B20 | Voice check-in save acked by coach, but card never updates and next beat never loads | 2 | tap path + post-fork advance preview-verified 2026-07-02 late; Vapi-live voice run still untested (Vapi off) | [!396](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/396) + [!398](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/398) | tap facet: state-check completion writes current_step=6, refresh lands the NEXT beat (verified earlier batch). Post-fork "never advances into the lane": the 2-min wedge was a WALKER artifact — wire evidence shows the tap advanced in ~100ms (ONBOARD-ADVANCED.mp3 fetch + form snapshot path=braindump + textarea rendered in every "stuck" screenshot); the walker matched the textarea PLACEHOLDER against innerText (never there) and kept classifying path-fork. The REAL residual hole (Vapi server-side submit_path_choice writes path with NO step climb → fork never advances) is fixed on a1a1178e: branch nodes advance on path evidence ARRIVAL via Realtime, entry-baseline so a back-nav is not yanked. Verified on gg-5cbfl0uc5: answered fork enters the lane in ~10ms, PUT lands ~1.5s, refresh inside the lane resumes into the lane (18/18) |
| B21 | Completed subcategory beat removed from timeline; habit-selection + habit-schedule render simultaneously | 4 | fix-implemented, preview recheck pending | [!400](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/400) | removal = B6/B7 unmount class; double-render its own sequencing defect |

## WORKLOG (append-only, one line per event; newest last)

- 2026-07-02 14:29 EAT — resumed prior session's run in a fresh session (Fable, xhigh); state recovered from STATUS.md + branches + !397
- 2026-07-02 14:40 — Loop 2 restart: re-verified GREATEST pin (api/onboarding/[...path].ts:37) + non-monotonic persist order 1,6,7,8,2,3,4,5,5 in generated flow
- 2026-07-02 14:50 — found: recordCheckin DOES write data.stateCheck (V3 comment in serverCaptureForBeat is stale); every persist beat has a data fingerprint → evidence-driven resume is viable
- 2026-07-02 14:55 — found: post-fork optimistic bump in useChatToolEvents is a no-op under the GREATEST pin (target 3..6 < pinned 8) → "dead air after path answer" (B4 facet) mechanism confirmed
- 2026-07-02 15:18 — resumeFromServerRow (evidence-first walk + numeric back-nav stop for steps 2-5) committed with flow-derived refresh-matrix test; 27/27 flow tests green
- 2026-07-02 15:20 — draft MR !398 opened (bugfix-loop2-resume → staging); B9/B10 fix-implemented
- 2026-07-02 15:35 — operator update: plan v2.3; reviewed MR !396 (card-fill) → docs/fix-reports/mr396-review.md; verdict stack-on-it; B20 fill wiring covered there, beat-completion + post-fork bump + 4 maps remain Loop 2
- 2026-07-02 15:40 — Loop 2 stacked on feat/onboarding-coach-card-fill (merge, no force-push); !398 is merge-after-!396
- 2026-07-02 15:45 — worktree node_modules lost to the dangling-symlink dance (B18 landmine); reinstalling; !396 untracks the symlink for good
- 2026-07-02 16:05 — operator: Vercel SSO off for previews; both loop previews load (200) — Chrome extension still dead (2x timeout), switching to Playwright
- 2026-07-02 16:12 — RULE-4 SANITY FAILED: loop previews, gg-qa-iota AND prod all bundle the PROD Supabase ref; supabase-environments.md §4 confirms Preview-scope env still prod-pointed. Interactive preview QA forbidden (writes would land in prod DB). Asked for Vercel Preview env fix + .env.staging.local. Team's gg-qa-iota walkthroughs have been writing to prod
- 2026-07-02 16:50 — HANDOFF to Yair's session: all branches pushed (loop1 fa04074f, loop2 1120062f, status current), STATUS rewritten with per-loop next steps + handoff notes; b11 request body still not received
- 2026-07-02 16:25 — no-write verification lane built: Playwright headless vs the deployed preview's auth-free /onboarding-flow-preview (in-memory persistence, zero DB writes); walker cleared IntroGate but auth beat walls it — added ?startAt=<nodeId> QA param to the preview route (fa04074f on loop1, cherry-picked to loop2), CI redeploying
- 2026-07-02 15:55 — Loop 2 batch 2 committed (62343899): four maps on V3 scale + flow-derived parity test; B20 completion (BEAT_COMPLETING += record_checkin/submit_morning_checkin, handlers GREATEST-bump, strictly-increasing bump w/ tool-to-beat guard); B2 voice forced ON at flow mount; B8 auth receipt suppressed; !396 allowedTools union guard picked up. tsc clean, 1365/1365 tests, build OK

- 2026-07-02 20:45 IDT — LOOP 2 SIGNED-IN VERIFY on gg-gxx5woha8 (auth driven via the real login form after finding raw-session injection rots: rotated refresh token → supabase signs out mid-run → PUT 401; one wedge was re-run with a fresh session to separate harness artifact from product). PASSES: refresh matrix state-check/morning/reflection all return the SAME beat with no extra components (screenshots), voice toggle ON after every refresh (B2), zero "You're signed in" banner (B8), B20 tap path (state-check card completion → current_step=6 on server → refresh lands on morning-setup, the NEXT beat). FAILS: (1) full beginner run auto-passes the UNANSWERED path-fork (radios empty in end screenshot) straight through plan-review + weekly-projection to into-app; flow "completes" with server path=null, step 8 — jump-to-end live; (2) fork answered by tap: path=braindump persists (PUT 200, server row confirms) but the client NEVER advances into the advanced lane (2-min wedge, reproduced with a fresh session) → the !398 post-fork/dead-air bump fix does not hold when current_step (6/8, normal at the fork in V3 order) exceeds the fork's target step; advanced-lane resume therefore UNVERIFIABLE. Also: profile beat opener fully silent on loop2 (no cartesia call — expected, B3 is !397). Evidence: /tmp/gg-verify/L2-R{1,2,3}-*-{result,wire,refresh-*}.json + screenshots.
- 2026-07-02 20:55 IDT — VERIFICATION BATCH COMPLETE; per-check table appended below; verdicts: !397 NOT ready to undraft (B4 playback race), !398 NOT ready (fork advance dead + live jump-to-end); both MRs' passing facets documented per bug row. Walker + harness preserved at /tmp/gg-verify (copy into docs/fix-reports/ if wanted for CI).
- 2026-07-02 21:35 IDT — LOOP 3 COMPLETE (B11/B12), MR !401 draft on bugfix-loop3-llm. B18 ruled out (staging tip: tsc 0 + build OK; an initial local failure was the stale shared node_modules missing the just-merged apple-sign-in dep, not a repo error). B11 root causes verified IN CODE (openai-responses.ts: unhandled response.incomplete terminal = silent empty finish; response.failed/error parsed with the wrong shape so every upstream error collapsed to "openai_error: Unknown error"; 600-token output default too small for tool-heavy onboarding turns) and LIVE (deterministic A/B, staging Supabase, qa-onboarding-fable: a 1990-char brain-dump turn fails 3/3 on the pre-fix loop4-tip preview with one upstream stream_error and zero output, succeeds 3/3 on the !401 preview gg-2cibz9jz6; the captured ONBOARD-BEGINNER-04 payload replays clean). Fixes: incomplete → retryable incomplete_response error frame; correct failed/error shapes; onboarding max_output_tokens 1000; [llm:fail] logging on every hard failure + x-gg-debug:1 debug echo (Sentry DSN confirmed UNSET on the API functions, so server failures previously left no trace); client 30s idle watchdog (retryable llm_timeout, no more endless thinking) + one silent auto-retry then ONE friendly bubble (raw codes never render as coach text) + failed openers degrade to the authored line + card. B12: no new change needed (Loop 2's ladder fix is the root of "confirm your path choice"; NO_INTERNAL_NARRATION already bans "I'll save that for you"; prompt-source grep clean). tsc + 1336 tests green. NOTE for preview owners: gg-qa-iota still bundles the PROD Supabase ref; the env-flipped branch previews are the only safe QA surface.

## Loop status

| Loop | Scope | Status | Branch | MR |
|---|---|---|---|---|
| 0 | Prior-fix archaeology | done | bugfix-status-2026-07-02 (docs/prior-fixes/) | — |
| 1 | B3 B4 B14 B15 | B4 REWORKED + preview-verified signed OUT 2026-07-02 (36963b50 settle-token race fix; allowed + blocked walker green on gg-bplca6yel); B3+B15 verified earlier; B14 gate unblocked (audible spot-check pending); temp diagnostics dae9bf1c REVERTED (354e0f79). tsc + 1365/1365 tests green. !397 ready to undraft pending the B14 listen | bugfix-loop1-audio | [!397](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/397) |
| 2 | B9 B10 B2 B8 B20 | COMPLETE 2026-07-02 late: the two batch FAILs (jump-to-end past unanswered fork + post-fork dead-end) fixed on a1a1178e and preview-verified 18/18 on gg-5cbfl0uc5; earlier passes (refresh matrix, B2, B8, B20-tap) intact. !398 ready to undraft AFTER !396 merges (stacked) | bugfix-loop2-resume | [!398](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/398) |
| 3 | B11 B12 | DONE 2026-07-02: B11 solved + live A/B verified (pre-fix 3/3 fail, fixed 3/3 pass, staging Supabase); B12 needs no new change (Loop 2 covers it). tsc + 1336 tests green. !401 draft, ready for review | bugfix-loop3-llm | [!401](https://gitlab.com/guidedgrowth-group/guided-growth-mvp/-/merge_requests/401) |
| 4 | B5 B6 B7 B17 | pending | — | — |
| 5 | B16 | pending (stacks on 1+2) | — | — |
| 6 | B1 remainder | pending | — | — |


## VERIFICATION BATCH — signed-in preview runs, 2026-07-02 evening (staging Supabase, user qa-onboarding-fable)

Loop 1 preview: gg-5h6ohbzcn (bugfix-loop1-audio, !397). Loop 2 preview: gg-gxx5woha8 (bugfix-loop2-resume, !398, stacked on !396).
Instrumentation: Playwright headless + HTMLMediaElement.play() tap (window.__playLog) + full console/network capture. Raw evidence: /tmp/gg-verify/.

| # | Check | Verdict | Evidence (one line) |
|---|---|---|---|
| 1 | L1 fresh signed-in run: every MP3 beat fetches (2xx) + reaches playback; Cartesia opener 2xx + plays | **FAIL (playback)** / pass (fetch, Cartesia) | All clips fetch 206; POST /api/cartesia-tts 200 + blob 'playing'→'ended'; but every MP3 opener logs `[useBeatOpenerMp3] activating` x2 then `play() rejected AbortError` and 0 'playing' events — beats settle silently (L1-R1-fresh-wire.json) |
| 2 | L1 mid-flow refresh at state-check: audio works after resume | **PARTIAL** | Post-refresh landed beat's Cartesia opener plays (2nd cartesia-tts 200 + 'playing'); MP3 beats still 0 plays (same race); resume itself landed on profile = loop1's numeric resume, loop2's scope (L1-R2-refresh-refresh-state-check.json) |
| 3 | L1 preload pool warms all 18 clips at flow mount | **PASS** | 18/18 `/voice/onboarding/*.mp3` 206 within ~150ms of mount, authed + preview routes, autoplay allowed AND blocked (wire dumps) |
| 4 | L1 autoplay blocked: no settle-as-done; defer to next tap | **FAIL (MP3)** / pass (Cartesia) | Hands-off at an MP3 beat: 2x AbortError → beat self-advances silently (probe-whyintro authed+preview, blocked+allowed); Cartesia opener: NotAllowedError → held un-settled → tap → 'playing' (L1-R3-blocked-result.json) |
| 5 | L2 refresh matrix (state-check 6 / morning 7 / reflection 8): SAME beat, no extras, voice ON | **PASS** | postBeat==preBeat x3 with clean single active card in screenshots; voiceMode='voice' post-refresh x3 (L2-R1/L2-R3 refresh JSONs + after-refresh PNGs) |
| 6 | L2 full beginner run: no skipped beat, no jump-to-end | **FAIL** | After reflection-setup the flow auto-passed the UNANSWERED fork → plan-review → weekly-projection → into-app; final server path=null, current_step=8; fork radios empty in L2-R1-matrix-end.png; one run also intermittently flashed past profile (recovered by refresh) |
| 7 | B20 live: state-check card completion advances current_step | **PASS (tap path)** | Server row current_step 1→6 after card Continue; refresh landed on morning-setup (NEXT beat, not earlier) (L2-R1-matrix-refresh-morning-setup.json) |
| 8 | B10: no jump over the in-between beat to profile | **PASS (resume path)** | Fresh run order auth→mic→profile→why-intro→state-check observed in L2-R3; caveat: live-advance jumps of check 6 are the surviving jump class |
| 9 | B8 no "You're signed in" banner; B2 voice toggle ON after refresh | **PASS** | signedInBanner=false in all loop2 probes; voiceMode='voice' + toggle leftActive after all 4 refreshes (result JSONs + PNGs) |
| 10 | Advanced + fork path: choose advanced, refresh inside the lane | **BLOCKED / FAIL** | Fork pick+Continue persists path=braindump (PUT /api/onboarding 200; server row confirms) but client never enters the lane — wedged 2 min, reproduced with a FRESH session (L2-R2-advanced-{result,wire}.json); lane refresh untestable |

**MR verdicts:** !397 **NOT ready to undraft** — B3 + B15 verified good, but the B4 playback fix is defeated on the deployed build by a double-activation race (activation #1's cleanup AbortError settles activation #2 on the shared pooled element → no MP3 opener ever plays, beats self-advance silently; fix hint: per-activation settle token + AbortError re-arms instead of settling). Temp diagnostics dae9bf1c also still on the branch. !398 **NOT ready** — refresh matrix / B2 / B8 / B9 / B20-tap all verified PASS, but the live-advance path still jumps to the end past an unanswered fork, and an answered fork persists yet never advances (post-fork dead air), so check 6 + the advanced lane fail.

Notes for whoever picks this up: /api/qa/users 500s on both previews (QA dropdown falls back to static list w/o the fable user — sign in programmatically or via the auth beat form); raw localStorage session injection is unreliable across runs (refresh-token rotation → mid-run sign-out → 401 writes) — drive the real login form; ?startAt=<nodeId> cannot cross the path-fork (fastForwardToNode walks empty captures and stalls at the unresolvable branch).

## HANDOFF 2026-07-02 ~16:50 EAT — run continues in Yair's session

**Per-loop state + EXACT next step:**

**Loop 0 — DONE.** docs/prior-fixes/ on this branch.

**Loop 1 (B3 B4 B14 B15) — fixes + tests in, verification partially blocked.**
Branch `bugfix-loop1-audio` @ fa04074f, draft MR !397. TEMP diagnostics commit
**dae9bf1c** (unconditional playback logging in useBeatOpenerMp3) — revert before
undraft. fa04074f adds `?startAt=<nodeId>` to /onboarding-flow-preview for
headless no-write QA. Fresh preview: https://gg-9uy17rwjz-guided-growths-projects.vercel.app
NEXT STEP: run docs/fix-reports/preview-walker.mjs (this branch) against
`<preview>/onboarding-flow-preview?startAt=profile` twice (autoplay-allowed +
default-blocked): assert every MP3 beat fetches its /voice/onboarding/<ID>.mp3
(200) and plays (temp diagnostics lines), Cartesia opener speaks on profile
(B3), no NotAllowedError settles-as-done (B4), preload pool warms clips at flow
mount (B15). THEN (after the Vercel env fix, see Blockers): signed-in fresh +
mid-refresh runs + Slow-3G latency on qa-onboarding-fable-loop1, revert
dae9bf1c, undraft !397.

**Loop 2 (B9 B10 B2 B8 B20) — ALL fixes implemented + unit-verified; preview
matrix pending.** Branch `bugfix-loop2-resume` @ 1120062f, draft MR !398,
**STACKED on !396** (merge after it). tsc clean, 1365/1365 tests, build OK.
Landed: evidence-driven resume (resumeFromServerRow + refresh-matrix test),
four V3 map remaps + flow-derived parity test (stepMapParity.test.ts), B20
completion (BEAT_COMPLETING += record_checkin/submit_morning_checkin, handlers
GREATEST-bump 6/7/8, strictly-increasing client bump + tool→beat guard), B2
voice forced ON at flow mount, B8 auth receipt suppressed, !396 allowedTools
union guard. Preview: https://gg-gd5e2zsf6-guided-growths-projects.vercel.app
NEXT STEP (blocked on the Vercel staging env fix): refresh at persist steps
6/7/8 → SAME beat, no extra components, voice toggle ON; full beginner run no
skipped beats; advanced + fork resume; live B20 (voice save on state-check /
morning advances the beat). Review docs/fix-reports/mr396-review.md first.

**Loop 3 (B11 B12) — NOT STARTED.** The captured failing habit-selection
request body was NEVER received (Yonas said he'd paste it — ask again; save as
docs/fix-reports/b11-request-body.json on this branch). First step: rule out
B18 (staging build state), then the CI-log server-side evidence plan in the
fenced block.

**Loop 4 (B5 B6 B7 B17 B19 B21) — NOT STARTED.** B17 root cause verified
(QAControlScreen restart never clears the client thread store). The auth-free
/onboarding-flow-preview + ?startAt is now available for no-write timeline
repros (B6/B7/B21 unmount class).

**Loop 5 (B16) — pending Loops 1+2.** Prior-fix notes in docs/prior-fixes/;
mic-arm + first-word variants already exist on feat/onboarding-voice-track1
(8de9b66, beed650), unmerged.

**Loop 6 (B1 remainder) — pending.** Reuse Loop 2's B2 fix for launch-with-voice-ON.

**Temp/WIP commit SHAs:** dae9bf1c (Loop 1 temp diagnostics — must be reverted
before !397 undrafts). No other WIP: every branch is fully committed + pushed.

## HANDOFF NOTES — machine-local, does NOT transfer

- **Worktree layout on the old machine** (recreate as needed):
  guided-growth-mvp (main checkout, operator's feat/onboarding-voice-track1 WIP
  — untouched by the loop), ../gg-bugfix-loops (this status branch),
  ../gg-loop1 (bugfix-loop1-audio), ../gg-loop2 (bugfix-loop2-resume),
  ../gg-flow-reference (flow-annotated-render oracle; dev server was :5199,
  `/flow-standalone/`). gg-spec cloned as sibling ../gg-spec.
- **node_modules:** installed with `npm install --ignore-scripts` (supabase-cli
  postinstall fails) + `npx tsc -b packages/shared` afterwards. On branches that
  still track the dangling node_modules symlink (loop1, status), the working
  tree shows a `D node_modules` — NEVER stage it (!396 untracks it properly).
- **.env:** only `.env.local` exists (PROD-pointed). NO `.env.staging.local`
  template exists on the old machine — staging keys were never local. Check
  whether Yair's machine has them (unblocks local staging verification +
  create-test-users.mjs).
- **Playwright:** v1.59 from the repo devDeps; `npx playwright install
  chromium` needed once (headless-shell v1217). The no-write walker is
  committed here as docs/fix-reports/preview-walker.mjs (edit the ROOT import
  path — it hardcoded the old worktree's node_modules; change to a plain
  `import { chromium } from 'playwright'` run from the repo root).
- **Claude-in-Chrome extension** was unresponsive on the old machine (2
  sessions); irrelevant if Yair's works, and Playwright covers headless needs.
- **Preview URLs churn per push** — always re-read the deploy_qa_preview job
  log (`Deployed: https://...`) after any push.

## Blockers

1. **ALL Vercel deployments point at PROD Supabase (pmunbflbjpoawicgimyc) — found
   2026-07-02 ~16:10 via rule 4's sanity check.** Branch previews (!397/!398),
   gg-qa-iota, and prod all resolve the same prod ref in their bundles; zero
   deployed surface uses staging (ppyouymvnrqxcsllrmsl). This is the documented
   §4 gap in docs/supabase-environments.md ("QA writes land in prod ... Not for
   tester data-entry yet") — meaning the team's live QA walkthroughs on
   gg-qa-iota have been writing to the prod DB. Interactive preview verification
   is FORBIDDEN until fixed (would create QA rows on prod). **Yonas/Yair: fill
   the Vercel Preview-scope env vars (VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY
   / DATABASE_URL / SUPABASE_SERVICE_ROLE_KEY) with the staging project's values
   per supabase-environments.md §4, then redeploy the loop branches.** SSO
   protection itself is now off (previews load without login) — env is the last
   wall.
2. **Staging Supabase keys not available locally:** `.env.local` holds the PROD
   ref; the `.env.staging.local` template from ENVIRONMENTS.md does not exist on
   this machine. Blocks: create-test-users.mjs on staging, and any local
   API-touching run against staging. **Provide `.env.staging.local` (staging
   URL / anon / service-role / DATABASE_URL) or run `npm run env:staging` setup.**
3. **Claude-in-Chrome extension unresponsive** (tabs_context times out; same
   failure as prior session — likely a pending permission prompt in the side
   panel). Workaround in use: Playwright headless for no-write checks.
   **Yonas: check the Chrome extension side panel / restart Chrome.**
- 2026-07-02 16:09 IDT — TAKEOVER: run continues in Yair's session; worktrees rebuilt under ~/Developer/claude-work; walker import made portable; starting Loop 1 no-write verification
- 2026-07-02 19:28 IDT — Loop 1 no-write verification: pool fix (preview route now mounts preloadOpenerClips, commit on loop1) verified locally: all 18 clips + splash + mic warm at mount; full chain walked to ONBOARD-COMPLETE; no NotAllowedError silent settles; AbortErrors are walk-speed artifacts. Env flip: 4/5 Preview rows updated; VITE_SUPABASE_URL Preview missed (still Jun 29) — fresh previews still bundle prod ref; asked Yair for the one-row fix. Starting Loop 4.
- 2026-07-02 19:44 IDT — ENV FLIP VERIFIED: all 5 Preview-scope vars on staging; rebuilt loop previews bundle staging ref x3 / prod ref x0 (loop1 gg-5h6ohbzcn, loop2 gg-gxx5woha8). QA user qa-onboarding-fable@guidedgrowth.test created on staging. Interactive preview QA is now SAFE. Signed-in verification batch starting.
- 2026-07-02 ~20:0x IDT — verification batch session up: staging sign-in REST OK (fable), /api/qa/self-reset on loop1 preview 200, RLS read of onboarding_states OK; NOTE /api/qa/users returns 500 on BOTH previews (dropdown falls back to static list; filed as observation, not a loop bug). Walker harness at /tmp/gg-verify (playwright from gg-loop1 node_modules; worktrees untouched). Starting Loop 1 signed-in runs.
- 2026-07-02 20:10 IDT — Loop 4 COMPLETE: B5/B6/B7/B19/B21 fixed+tested on !400 (B21 root cause included fastForwardToNode burning the flow to completion; B7 was the voice-beat card-loss class). Full suite 1353 green. Preview rechecks folded into the running verification batch.
- 2026-07-02 20:20 IDT — LOOP 1 SIGNED-IN VERIFY, MAJOR FINDING (revises the 19:28 "AbortErrors are walk-speed artifacts" read): on gg-5h6ohbzcn, EVERY useBeatOpenerMp3 beat activates TWICE (~same ms), cleanup of activation #1 pause()s the shared pooled element, its late AbortError then settle()s activation #2 → every MP3 opener is marked done WITHOUT EVER PLAYING (playLog instrumentation: 0 'playing' events for any onboarding mp3; say-only beats like why-intro self-advance hands-off in <100ms). Reproduced hands-off on BOTH /onboarding/flow (signed in) and /onboarding-flow-preview, autoplay allowed AND blocked. B4's settle-as-done symptom is therefore still live via AbortError (the NotAllowedError defer itself is correct: the profile Cartesia opener under blocked autoplay defers → next tap → plays to completion, verified). B15 preload PASS (all 18 clips 206 within ~150ms of mount, both routes, both policies). B3 Cartesia opener PASS (POST /api/cartesia-tts 200, blob plays + ends). Evidence: /tmp/gg-verify/L1-R1-fresh-{result,wire}.json + probe-whyintro-*.json.
- 2026-07-02 20:35 IDT — Loop 1 signed-in batch continued: (a) mid-flow refresh at state-check → resume lands on PROFILE (server step=1, numeric resume; B9 live on loop1 as expected, loop2 owns it); the landed beat's Cartesia opener plays post-refresh (2nd /api/cartesia-tts 200 + playing), MP3 beats still 0 plays. (b) Settle cascade corollary: say-only beats self-advance, so after reflection (step 8 GREATEST pin) the flow catapults past fork+lane and auto-runs plan-review + ALL FIVE weekly-projection beats + completion as silent instant text bubbles (screenshot L1-S-lane-stuck-12.png). (c) ?startAt=<lane node> can't cover BEGINNER-01..04: fastForwardToNode's empty captures wall at the unresolvable path-fork branch, then the cascade burns the shared tail — lane MP3s unreachable signed-in on loop1; their fetch evidence stands via the 18/18 preload. (d) Zero [opener] 401/500 in any run; /api/cartesia-tts 200 x4 across runs. VERDICT FORMING: !397 NOT ready to undraft — playback facet regressed/unfixed on the real route (mechanism above); B3+B15 facets verified good.
- 2026-07-02 20:47 IDT — B11 evidence received (failing habit-selection request body, captured 10:52-11:01 UTC session e6988bc9); saved as docs/fix-reports/b11-request-body.json. Payload is well-formed; strengthens server-side failure theory. Loop 3 starting. Also: auth (Google/Apple) merged to staging as own work; OAuth providers still unconfigured on the staging Supabase project (dashboard config, flagged).
- 2026-07-02 21:45 IDT — [loop1-fix] B4 race FIXED on bugfix-loop1-audio (36963b50): root cause confirmed exactly as the 20:20 finding (double activation; run #1 cleanup pauses the shared pooled element; its late AbortError settled run #2). Fix = per-activation settle tokens (openerActivation.ts, pure, 18 new tests incl. a StrictMode hook race repro that fails on the old code), AbortError re-arms on the live activation (bounded 2 retries), pool claim/release serializes the warm-element handout, BeatView holds karaoke at 0 while audio is armed-not-started (say-only beats can no longer advance silently; 12s reveal safety kept). dae9bf1c temp diagnostics REVERTED (354e0f79), one quiet warn per failed clip kept. tsc clean, 1365/1365 vitest. Verified locally (vite :5323) AND on the CI preview gg-bplca6yel signed OUT, both via Playwright walker (/tmp/gg-verify/loop1-fix-walker.mjs): allowed = 6 MP3 beats (why-intro through category) each with a 'playing' event BEFORE the next beat armed, zero ended-without-playing; blocked = why-intro holds hands-off, trusted tap starts playback, plays to end, advances. B14 gate now exercised (audible listen pending). Verdict: !397 ready to undraft pending that listen. Fix report updated on the branch (docs/fix-reports/loop1-audio.md, cd2b5d1e).
- 2026-07-02 21:10 EAT [yonas-session] - second engine joined (Fable, xhigh). MR !396 review support done for Yonas's GitLab review: staging-sync merge ca7c859f verified clean (QAControlScreen = staging's one-tap version byte-identical; componentRegistry keeps only the ReflectionAdapter prompts delta on top of staging's Apple sign-in), designerToFlow churn confirmed reflow-only by word-diff, allowedTools overlay net effect verified (only ONBOARD-COMPLETE gains update_habit), CI green at tip. NEW: the "unproven serverless JSON-import lane" risk is now ruled out at module-load level - unauthenticated POST /api/llm on the loop2 preview (bundles !396) returns app-level 401 no_token, not a cold-start crash. Residual: authed tool-gate exercise on an export-sourced beat still untested; empty-after-filter edge in the overlay guard (raw length checked, filtered list assigned) is covered by !398's union guard, or a one-liner in !396. Starting Loop 6 (bugfix-loop6-qa-audit off origin/staging); not touching loops 1-4.
- 2026-07-02 21:35 IDT — [loop2-fix] fork live-advance FIXED on bugfix-loop2-resume (a1a1178e). FAIL-A (jump-to-end past the unanswered fork) root-caused from the batch evidence: the reflection save's step-8 climb (fire-and-forget + GREATEST pin) lands AFTER the machine enters the branch node, the leading-edge advance replays the fork's server capture (no path), and flowMachine's branch merge-fallthrough carries it to into-app + the weekly tail. Fixed two layers deep: captureCompletesBeat gate on the live advance (kept identical to !400's B21 gate for a clean merge; once-per-beat latch consumed only on a real advance) + applyCapture hard stop at an unanswered branch (fallthrough now an explicit branchFallthrough option used only by the QA fastForwardToNode walk, so merge-side ?startAt targets keep working). FAIL-B ("answered fork never advances") re-diagnosed from the wire dumps as a WALKER artifact: the tap advanced in ~100ms every time (ONBOARD-ADVANCED.mp3 fetch, snapshot path=braindump, textarea in every stuck screenshot); identifyBeat matched the textarea placeholder against innerText (placeholders never render there) then hit the frozen fork radios rule forever. The REAL residual dead-end it was masking (Vapi's server-side submit_path_choice writes path with NO step climb, so the climb-based advance never fires) is fixed: branch nodes now advance on path evidence ARRIVAL via Realtime, with an entry baseline so a voice back-nav to an answered fork is not yanked forward. New flow-derived suite liveAdvanceFork.test.ts (12 tests: race precondition, hard stop, both lanes via tap AND via no-climb server path, full-lane run, startAt semantics); tsc clean, 1379/1379. VERIFIED signed-in on the new preview gg-5cbfl0uc5 (staging ref x3/prod x0 in bundle): full beginner run holds at the fork 12s hands-off (server path=null, no into-app/weekly markers), answering enters the lane in ~10ms with the PUT landing ~1.5s (path=simple and braindump), refresh at state-check(6)/morning(7)/reflection(8) returns the same beat, refresh inside the lane resumes INTO the lane through the answered fork, and a plain reload of a startAt-jumped session rewinds to profile (no jump-to-end). 18/18 checks; evidence /tmp/gg-verify/FORKFIX-* + fork-verify.mjs. Fix report docs/fix-reports/fork-live-advance.md on the branch. Verdict: !398's two batch blockers are cleared — ready to undraft once !396 merges (stacked). Harness notes for the next verifier: force a FRESH REST sign-in per browser run (refresh-token rotation kills a reused injected session mid-run) and never reload within ~2s of a card tap (it aborts the fire-and-forget PUT).
- 2026-07-02 21:44 IDT — HUMAN LISTEN on the !397 fix preview (gg-bplca6yel): beats 2-3 audibly good (first human confirmation of the audio fix). NEW BUG (B22 candidate): the run breaks right after ALLOWING THE MICROPHONE on the preview; symptom detail pending from Yair. All prior verification ran voice-in OFF, so the mic-grant path is exactly the untested seam (soniox key route / STATE3 flag / preview auth gap). Route to Loop 5 scope or a quick standalone check.
- 2026-07-02 21:50 IDT — OAuth providers (Google + Apple) configured on the STAGING Supabase project (done by the team). Google/Apple sign-in is now testable on staging-pointed previews; the earlier caveat is retired.
- 2026-07-02 21:56 EAT [yonas-session] - LOOP 6 COMPLETE (pending one !398-gated recheck). Branch bugfix-loop6-qa-audit off staging 9526b377, draft MR !402, fix report + audit table in docs/fix-reports/loop6-qa-audit.md on that branch. Live-verified on preview gg-415l647na (staging-ref bundle checked first; fable user; Vapi off): 10/11 audit rows PASS. Fixed: preview tiles (home tour/morning/evening) no longer WIPE the picked user's server data before opening an in-memory preview (destructive no-op, FlowDef.kind gates the reset); "Log in" now routes via '/' (AppGate) matching its label; "Restart onboarding (fresh)" always runs full onboarding; "Re-run onboarding (keep data)" (lied for completed users, AppGate bounced them home) converted to "Replay flow (preview)" -> /onboarding-flow-preview; "Reset data only" stays on the QA screen with a confirmation (was landing the wiped user INSIDE onboarding); fallback user list leads with the two real staging accounts. NEW LATENT BUG B23 for the ledger (labeled B23 because the 21:44 IDT line already claims B22 for the mic-allow break): switching QA users never clears the react-query cache (only signOut does; gate query staleTime Infinity) so a warm-heap launch reads the previous user's or pre-wipe row; latent because every launcher entry is a fresh page load today, armed by multi-action visits (incl. the new stay-put reset); fixed in !402 via queryClient.clear() before nav, warm-heap fresh-state-read proven live. Expected small run() conflict with !400, resolution documented in the MR. One FAIL: "Mic + Profile" tile lands on PROFILE (mic skipped) = B10 class (up-front nickname persist + numeric resume overshoots the seeded startAt stop; MicPermissionAdapter has no auto-skip, headless artifact ruled out) - owned by !398, NOT duplicated in !402; re-verify that tile + the voice-ON-launch requirement after !398 merges. Stale observation cleared: /api/qa/users no longer 500s on previews (200 with Fable+Mintesnot; the logged 500s were pre-env-flip). Loop 5 still gated on !397+!398 merging.
- 2026-07-02 22:10 IDT — [gesture-audio] Gesture-gated first audio SHIPPED on feat/gesture-gated-first-audio (stacked on bugfix-loop1-audio, draft MR !403, merge after !397). The Get started tap now starts splash_welcome.mp3 SYNCHRONOUSLY in its own gesture frame (new startOpenerFromGesture: pool claim + play, no readiness await) and SplashIntro ADOPTS the already-playing element (new adoptedOpener prop) instead of re-arming play(), so the first clip can never be autoplay-blocked and fire later on a random unrelated tap; the in-gesture play doubles as the audio unlock for all later beats. Deferred-to-tap fallback kept for ?startAt/QA-tile entries; no other beat touched; done-after-playing preserved. ALSO on this branch, the 21:44 B22 mic-allow break: root cause = preview anonId "preview-<uuid>" vs uuid-typed anon_id columns (every anon-keyed REST 400d) + the mic beat awaiting an upsert that rejected uncaught (spinner wedge). Fixed: plain-uuid preview seed (marker moved to localStorage gg_preview_anon_id) + useUserPreferences degrades a failed remote upsert to local-only (resolves, values stick, beat advances; protects real users on RLS rejects; Settings mic-toggle error toast no longer fires on remote failure, value applies locally instead). tsc clean, 1375/1375 vitest (8 new tests incl. adopt-not-double-start + degrade seam). Verified via Playwright with autoplay BLOCKED (default chromium) on local dev AND the CI preview gg-m3a7gedqs: 12/12 checks; no audio on earlier/unrelated taps, play() +42ms and playing +99ms from the tap, clip runs to its 13.28s end, mic beat MP3 plays with no extra tap, Allow advances with no wedge, zero uuid 400s, zero uncaught errors. Fix report docs/fix-reports/gesture-first-audio.md on the branch. Yair's assumption (same functionality, deterministic start) HELD; the one deliberate behavior change is the Settings degrade noted above.
- 2026-07-02 22:32 IDT — DB password rotation VERIFIED end to end: rotated in Supabase, vaulted, Vercel Preview DATABASE_URL updated, fresh deploy (gg-2iu5hef3k) self-reset returned 200 with real row deletions. First attempt failed only because the deploy predated the row save (env is baked at deploy time). qa-onboarding-fable account is freshly wiped.
- 2026-07-02 23:05 EAT [yonas-session] - !396 SMOKE PASS + MERGED; train parked at !398. (1) Authed gate smoke on a fresh post-rotation preview of !396 (gg-lnzcodh30): model CALLED update_habit on ONBOARD-COMPLETE (export-only tool there; code-owned list was confirm_plan alone) - SSE 200, 3.2s, tool_result ok; JSON-import lane fully retired; noted on the MR. (2) /api/qa/users 500 root-caused: pre-rotation previews hold the old DATABASE_URL (Vercel env changes apply to new builds only) so the onboarded-badge enrichment query throws and killed the whole list; fix on !402 (372915a0): enrichment failure now degrades to a bare list + unit tests; fresh deploys already 200. All six QA accounts (five named + fable) exist on staging and sign in with the QA password - recreation was already done with the 22:32 rotation work, no action needed. (3) MERGE TRAIN: local full gate at the simulated post-merge tree (staging+!396: tsc clean, 1360/1360, build OK) then !396 merged to staging (16270b72); note staging also gained a CI verify stage (!404: npm ci + tsc + vitest + build gating deploys on every push). bugfix-loop2-resume updated with merged staging (clean merge, no conflicts, 8edd84c7 pushed) - !398 is next in line, still DRAFT, waiting on its review to land; !400/!401/!397/!403/!402 likewise drafts. Post-merge re-verification batch (Mic+Profile tile, voice-ON launch, B12, B20 voice, full beginner run) queued for after the train completes.
- 2026-07-02 23:00 IDT — Team-side infra batch landed (other session): Apple+Google auth LIVE on staging Supabase (same Services ID as prod, staging return URL in the Apple portal); staging DB migration-synced (054 applied); the five named QA accounts recreated on staging and visible in the dropdown; /api/qa/users 500 FIXED (root cause: DB password containing dollar-sign and paren pasted un-encoded into the guided-growth-mvp project's Preview DATABASE_URL; now URL-encoded, verified 200). Environment map clarified: TWO Vercel projects exist; both gg-qa (flipped+verified tonight) and guided-growth-mvp Preview scopes are now staging-backed. The ONLY remaining prod-pointed surface is the stale gg-qa-iota alias build, which self-heals on the next staging-branch push (first merge of the train).
- 2026-07-02 23:14 EAT [yonas-session] - POST-MERGE PIPELINES GREEN x2. Staging pipeline on 16270b72 (staging + !396) passed the new CI verify stage (npm ci + tsc + vitest + build) and deployed: gg-qa-iota now carries !396. bugfix-loop2-resume's pipeline on 8edd84c7 (updated with merged staging) also green end to end; fresh !398 preview: gg-9tj7pb1yf (post-rotation env, staging DB) - ready for its review/undraft, then merge. Train remains parked on draft status of !398/!400/!401/!397/!403/!402; conductor merges also need an explicit operator go per the session's permission gate.
- 2026-07-02 23:08 IDT — MERGE TRAIN ROLLING: !396 merged (16270b72) with authed tool-gate smoke passed; CI verify gate live (!404); gg-qa-iota rebuilt and VERIFIED staging-pointed (staging x3 / prod x0): zero QA surfaces write to prod anymore. loop2 branch synced with merged staging (8edd84c7). !398/!400/!401/!402/!403 all undrafted; merge order !398 -> !400 -> !401 -> !397 -> !403 -> !402, conducted from the second session. Loop 5 unblocks at !397+!398 merged. frugal-fable skill vendored into gg-spec/skills (supply-chain note actioned, pin 8d93ef1c).
- 2026-07-02 23:33 IDT — !398 review edges FIXED on the branch: profile evidence now requires age AND gender (partial-fill resume trap closed, tests both directions), and the QA startAt seed suppresses the resume walk entirely (closes the race behind the Mic+Profile tile intermittency). 1381/1381, tsc clean, MR noted. !397 merge-time cautions on record: stop-and-check rebase (not on the documented-conflicts list) and merge !403 immediately behind it (B22 lives on !397's surface, fixed in !403).
- 2026-07-03 10:23 IDT — B24 (from Mint's handoff, code-level proven, runtime check pending): on ONBOARD-STATE-CHECK the interim STT barge-in aborts the in-flight LLM reply (~100-300ms) before the late record_checkin tool frame (~2.3s), so the card never fills on the spoken path. This is exactly the recorded B20-voice-facet blind spot. Latent BUG 2: React batching can swallow tool_result+done arriving in one chunk. Fix direction: port the check-in half-duplex pattern (86e0adb5) to the onboarding voice path + defensive batching fix, coordinated WITH Loop 5 (same turn-loop surface). Note: 0640a4f5 cited in the handoff is on unmerged !401. Full analysis: Mint's handoff doc.
- 2026-07-03 11:15 IDT — [conductor: planning session] MERGE TRAIN ROLLING, machine-gated on Yair's explicit go (reviews waived for the train; human QA follows on the merged build). !398 MERGED (staging c4ba24b7 green). !400 MERGED after resolving its documented conflict with !398 on the branch (6c818d5f: fastForwardToNode = lane-aware seeding + branchFallthrough; one captureCompletesBeat; staging's answered-fork effect kept; state-check proxy doc-test updated to assert !398's deliberate proxy removal; 1412/1412) — staging f0de6d3c green. !401 MERGED (staging a9f3870b green). !397: stop-and-check done as a local inspection merge in a fresh worktree (gg-merge-397; BeatView = hasOpenerAudio guard + openerTurns split composed, preview keeps B15 warm-up; 1461/1461 green), pushed 3c151af7, MR undrafted, CI running. REMAINING: merge !397 on green -> staging green -> !403 immediately behind (expect same node_modules-symlink landmine on its branch; sync + gate + merge) -> !402 (documented small run() conflict with !400, resolution in the MR description) -> staging green -> AUTOMATED QA BATCH on the merged build (walkers in /tmp/gg-verify + gg-status/docs/fix-reports/*.mjs, playwright in gg-loop1 and gg-merge-397 node_modules, fable QA user) -> QA report to Yair. Lane kickoffs are HELD until Yair sees the QA answers (his order). Overnight team merges noted: !405 prod-DB guard, !406 QA password env-only (both reviewed clean). Mint's account confirmed as the builder-lane slot (4% weekly, 0% Fable, resets Jul 9 21:00 Addis).
- 2026-07-03 12:05 IDT — [conductor] TRAIN COMPLETE + AUTOMATED QA BATCH COMPLETE on the merged staging build (gg-lew563cg1, staging-ref bundle verified, prod-ref hit = !405's guard constant). AUDIO: all green both autoplay policies (6 MP3 beats each with a real playing event before the next armed, zero silent settles, blocked-autoplay holds hands-off and recovers on tap; gesture-gated first clip verified by the blocked scenario). FORK/RESUME MATRIX: 17/18 twice (fork holds unanswered 12s, both lanes enter <25ms with server rows confirming, refresh at 7/8 + mid-lane + startAt-reload all correct). QA SCREEN: loop6 walker 12/12 (all 6 accounts in dropdown, preview tiles wipe nothing, restart-fresh wipes + lands clean, warm-heap fresh state read, Mic+Profile lands with UI probe at startAt=mic). B12 process-talk grep across all fresh transcripts: clean. NEW LEDGER ROW B24: refresh DURING state-check (before completing it) rewinds to PROFILE instead of returning to state-check; reproducible 2/2 by walker, 27s+ after profile submit; refreshes at morning(7)/reflection(8) pass, so profile evidence EXISTS by then; suspected: profile server write landing late or a text-path persist gap that backfills only on the next beat completion. Loop-2 territory, anchor queue; NOT demo-blocking (edge: refresh inside one specific unanswered beat; costs the user a re-answer of 2 profile questions). Remaining cells delegated to the human walkthrough: audible first-word quality (B14 spot), voice-toggle-ON eyeball (step 3), brain-dump live turn (step 12; B11 also has yesterday's 3/3 live A/B on !401's preview). Human walkthrough is GO and now requires narrated screen recording with console open -> "GG QA Recordings" Drive folder (watcher transcribes).
- 2026-07-03 14:0x IDT — HUMAN WALKTHROUGH RESULTS (beginner video processed; advanced video uploading): 7/17 pass. REGRESSIONS ON MERGED STAGING: (R1, demo-blocking) advanced lane FREEZES after brain-dump submit with NO retry UI = B11 class back, despite !401 (my automated batch never exercised the submit; human caught it). (R2, demo-blocking) welcome opener + mic beat SKIPPED on fresh entry straight to profile (fresh browser) = B10 class back; suspicion: pre-existing nickname in user metadata -> up-front persist -> numeric-resume overshoot on fresh entry; the wiped fable account does not reproduce, the lived-in tester account does (data-dependent, same reason the state-check refresh inverts between accounts: tester PASSES it, wiped fable account FAILS to profile = B24). Card partials: merged profile card + unspoken profile messages + extra opener (B5/B7 recheck), morning-setup refresh renders NO component + reflection refresh renders EXTRA components (B9 class), habit-schedule invisible until coach prompt then empty despite 2 configured habits (B20/!396 class), NEW B25: plan-review renders selected habits as plain text, card missing. One beat silently skipped mid-flow (timestamped in video). Step 17 not covered. Conductor starting R1 root-cause via the captured b11 request replay against the deployed build with x-gg-debug. MERGE HOLD stays for lane MRs; fix MRs exempt once verified. Production promote GATED on R1+R2.
- 2026-07-03 14:3x IDT — [conductor] R1/R2 INVESTIGATION RESULTS. R2 (welcome+mic skipped on fresh entry) ROOT CAUSE IDENTIFIED and reproduced on BOTH a lived-in and a fully wiped account: any auth user with a nickname in metadata gets an up-front persist row at entry; resumeFromServerRow treats ANY server row as a resume and walks through the pre-step beats (auth/mic/welcome) by design, stopping at profile (age+gender evidence missing). A nickname-only row must be treated as FRESH, not resume. Affects EVERY real signup (nickname captured at auth), so this is prod-critical, not QA-only. Fix target: the resume-walk engage condition in useFlowOrchestrator (resume only on meaningful progress evidence, not nickname-only rows). R1 (advanced-lane freeze): NOT reproducible on a clean account; three probes show the full chain healthy end to end (dump submit -> PUT 200 -> frequency beat -> Looks good -> plan review; both server legs also verified directly: /api/llm streams clean, parse-brain-dump 200 in 3.2s). NEW B26 while probing: the engine's advanced path never calls the LLM parse at all; habit cards form from the naive local text split (produced "the mornings" as a habit name). Quality bug, likely the tester's "confusions". R1 still needs the FULL advanced video (the processed one is a 90-second partial upload; re-upload needed if Drive holds only the partial) or the tester's account state. Self-reset scope confirmed: wipes onboarding_states/user_habits/chat_messages/session_log + NULLs profile onboarding fields; SURVIVES: auth metadata (nickname), user_preferences.
- 2026-07-03 15:3x IDT — R2 FIXED, VERIFIED, MERGED (!417). resumeFromServerRow now treats a no-progress row (nickname-only, the up-front sign-in persist) as a FRESH start; progress = any beat-owned field including partial fills (gender-without-age still resumes AT profile per the !398 review edge). 3 new tests + matrix expectations corrected (old ones codified the bug); 1476/1476, tsc clean. LIVE verified on the !417 preview: fresh entry on a nickname-carrying account renders get-started -> MIC BEAT -> profile. Merged to staging machine-gated; gg-qa-iota rebuilds with it. PROMOTE GATE: R2 closed; R1 remains (unreproducible on clean accounts across 3 full probes + healthy server legs; awaiting the FULL advanced video re-upload, the processed one is a 90s partial). QA ROUND 2 on the new staging build is now warranted: rerun the automated batch + the human walkthrough after this deploy goes green.
- 2026-07-03 16:0x IDT — SECOND ADVANCED-PATH VIDEO ANALYZED (full, narrated). NO R1 FREEZE in this recording either: the brain-dump chain completes normally in the console; the closest moment is a follow-up habit-suggestion request getting no proper LLM answer (B26 class, the LLM-less advanced path). 5 findings pushed (3676dd6): 3 confirm knowns (an mp3-mute moment noted as B4-class, two advance_step tool_failed occurrences B11-class that recovered, B26 no-LLM-response), 2 new small (evening-reflection AI-learning confusion; an unnecessary chart bubble, possibly B21-overlap). None demo-blocking. TIMING CAVEAT: recording time vs the train's deploy is unknown; pre-train recordings describe the old build, so the B4/B11-class confirms may be stale. R1 EVIDENCE BALANCE now: one verbal report + a 90-second partial video AGAINST three clean conductor probes + this full recording with no freeze + both server legs verified healthy. RECOMMENDATION to Yair: downgrade R1 from demo-blocking to monitoring (the !401 retry UI + 30s watchdog already bound the failure if it exists), make B26 the real advanced-path work item, and re-promote R1 only if the full original video lands showing otherwise. Promote gate would then be clear.
- 2026-07-03 16:2x IDT — QA ROUND 2 on post-!417 staging (gg-mkzppj7sa): R2 fresh-entry PASS (get-started -> mic -> profile), audio ALL PASS, fork/resume 17/18. The recurring "refresh at state-check lands earlier" is RECLASSIFIED: it lands on why-intro, the contracted conservative fallback (state-check leaves no trace until its save; the resume test matrix explicitly allows why-intro there). B24 closes as designed-behavior; the walker's assertion was stricter than the contract and gets relaxed. AUTOMATED BOARD IS FULLY GREEN. Remaining before promote: Yair's bless on the R1 downgrade-to-monitoring, then the promote sequence itself (reconcile main, release note, his go, prod deploy + greps + signup e2e + Vapi cap revert).
- 2026-07-03 16:5x IDT — YAIR DECISIONS: (1) R1 DOWNGRADED to monitoring (same-build analysis: no code changed between the reported freeze and three clean probes; leading theory is B26 perceived as a freeze, the LLM-less advanced path giving no coach response; !401's watchdog+retry bounds a real LLM death; any recurrence re-escalates). (2) MERGE HOLD LIFTED: conducting the lane train !408 -> !412 -> !413 -> !414 -> !416 -> !418 -> !410 -> !419 -> !409, machine-gated per merge; !411 parked until the builder session rebases it onto the landed chain, then merges last. (3) PROMOTE PARKED by Yair: production waits until the QA app is declared perfect; staging remains the only moving surface; promote prep notes stay current so the eventual step is boring. Builder lane's preview walks unblocked by lending the fable QA account (conductor runs parked meanwhile).
- 2026-07-03 17:1x IDT — B27 FILED (renumbering the builder lane's "B24" report; B24 was already taken and closed): the floating voice-orb overlay (pointer-events-auto) COVERS bottom CTAs at phone viewport (375x812) and swallows real taps; reproduced by the builder lane ON STAGING at the state-check Continue (elementFromPoint at CTA center = the orb div; a JS .click() advances fine). R1 LINKAGE, probable mechanism found: every conductor probe clicked via JS dispatch (bypasses hit-testing) and desktop viewport, exactly the blind spot; a human on a phone tapping "Looks good"/Continue under the orb gets swallowed taps = "frozen, can't go forward" with no error, no spinner, no retry (matches the R1 report precisely, and the second tester ran desktop-with-console where the geometry differs). R1 stays in monitoring but is now EXPLAINED-PENDING-FIX-VERIFICATION. Fix = anchor territory (overlay hit-region: container pointer-events-none, interactive elements only auto, plus scroll-area bottom clearance); fix verification = real-click phone-viewport walk, which doubles as the R1 confirmation. Lane train meanwhile: !408 !412 !413 merged, conducting continues.
- 2026-07-03 18:0x IDT — LANE TRAIN COMPLETE (9/9): !408 !412 !413 !414 !416 !418 !410 !419 (builder-to-engine) + !409 (context anti-improvisation) all merged to staging, green after each. Staging now carries: hardened builder->engine pipeline (zod boundaries, typed registries, flow registry, linear transform, per-flow persistence, canonical artifact, mirrors demoted), morning + evening check-ins AND the app tour shipping from builder Exports, plus the anti-improvisation context work. !411 (derived step maps) still parked for its rebase onto the landed chain, merges last. Both QA users live (fable-builder + fable-latency confirmed). NEXT: QA round 3 (automated) on the combined build, then the B27 orb-overlay fix (branch off this final tree) with a real-click phone-viewport verification that doubles as R1 confirmation. Promote still parked per Yair.
- 2026-07-03 18:2x IDT — B27 FIXED (!420 draft), R1 RESOLVED-BY-EXPLANATION. Definitive before/after at phone viewport, same state-check Continue: BEFORE elementFromPoint@CTA-center = orb div (intercepted), AFTER = the Continue button span (CTA now above the orb, y 731->552). Fix = scroll-anchored clearance spacer replacing trailing padding that scrollIntoView(block:end) had scrolled away. Shared FlowRenderer, so onboarding + both check-ins + tour all covered. R1 (the reported advanced-lane freeze with no error/spinner) is now MECHANICALLY EXPLAINED: a human phone tap on a CTA under the orb did nothing; conductor probes used JS-dispatch (bypasses hit-testing) + desktop viewport, the exact blind spot. B26 (advanced path skips the LLM) remains the separate real advanced-path work item. Recommend one human phone tap on the !420 preview as the final seal; then merge (machine-gated, hold-exempt) at Yair's word.
- 2026-07-03 18:3x IDT — B28 FILED (from Yair's live phone check on the !420 preview): under real MOBILE autoplay blocking, a beat opener holds waiting for a trusted tap (correct, the B4 fix's deliberate hold) but shows NO tap-to-play affordance; the screen reads as STUCK at the first coach bubble ("Hey," + orb, no buttons). First-impression risk for any phone user including the advisor. The improvement plan already scoped a tap-to-play affordance for the check-in reveal (Phase 1 item 7); B28 generalizes it to ALL beat openers: when a clip is armed-not-started due to autoplay rejection, show a visible "tap to play" affordance (or auto-fall-back to text reveal after a short hold). Not a regression of !420 (the orb-clearance before/after stands); separate fix, high priority for phone UX.
- 2026-07-03 18:4x IDT — B28 REFINED per Yair's diagnosis (correct): the !403 gesture-gated audio unlock is wired ONLY to the main route's Get started tap; the /onboarding-flow-preview route, the QA tiles, and the mid-flow REFRESH entry deliberately kept the deferred-to-tap fallback WITHOUT an unlock gesture or any affordance, so on real mobile their first clip autoplay-blocks and the beat holds looking stuck ("Hey," + orb, no buttons, no hint). Fix scope (anchor, after !420 merges): (1) extend the in-gesture opener start / audio unlock to every entry that HAS a first tap (preview IntroGate's Get started, QA tile launches), (2) for entries with NO gesture (mid-flow refresh landing directly on a clip beat), show a visible tap-to-play affordance on the held opener, with a text-reveal fallback after a short hold so the flow never reads frozen. The phone walkaround for testers meanwhile: tap the bubble once, or toggle sound off.
- 2026-07-03 19:0x IDT — !420 MERGED after Yair's human phone seal ("tap works": profile Continue + onward respond to normal finger taps on the fix preview). Staging green; gg-qa-iota now carries the COMPLETE day: bugfix train, R2 fresh-entry fix, full builder-to-engine lane (check-ins + tour from builder Exports), context anti-improvisation, and the B27 orb-clearance fix. R1 CLOSED (mechanism = B27, fixed, machine-proven + human-sealed). TEAM QA ROUND 2 IS GO on this build. Remaining open: B28 (gesture audio unlock missing on preview/QA-tile/refresh entries, anchor's next fix), B26 (advanced path skips the LLM, needs scoping), B25 (plan-review card), !411 (rebase pending), !415 (latency spans verifying). Promote parked until round 2 verdicts.
- 2026-07-03 19:4x IDT — THREE TIMOTHY VIDEOS ANALYZED (summary + per-run findings pushed, d29aa26; note: two of the three are duplicate watcher re-ingests of the same Drive files, filed once). BUILD: post-lane-train (~18:45, check-in/tour tiles present), post-R2, DESKTOP captures (ASUS laptop), so the B27 orb geometry was untestable in this footage and predates/bypasses the !420 question. NEW LEDGER ROWS: B29 = caption box renders as a BLANK WHITE RECTANGLE for the clip's duration while audio plays (pattern confirmed at three independent timestamps; this is the scoped-but-unfixed duration-metadata reveal gate, now field-confirmed; fix pairs with B28, same reveal/audio-gate family, one MR). B30 = create-habit button on HOME is dead (post-onboarding day-one surface; advisor-relevant). B31 = AI stops responding after a mic pause/resume (voice-session lifecycle, high severity if the demo uses voice; B16-adjacent, not a clean match). Low-confidence: duplicated coach line before the B31 stall. CONFIRMS: B26 third independent confirmation (advanced path raw text, no LLM parse); B4-CLASS RECHECK NEEDED: two overlapping audio sources reported on the welcome opener on the post-train build (if reproducible on CURRENT staging it is a live regression candidate, verify in the next automated batch). ANCHOR FIX QUEUE: (1) B28+B29 combined MR, (2) B30 triage, (3) B31 triage. B26 scoping remains the top product item.
- 2026-07-04 morning — !421 MERGED (B28 + B29 fixed): built overnight by the builder-lane session on the conductor's add-on task, conductor-verified on preview under blocked autoplay at phone viewport (gesture pool blessing 22 in-gesture plays; Tap-to-play pill on held openers after a signed-in refresh; text reveal past the 4s hold window; real tap starts audio; B4 invariant preserved). Staging rebuilding. DEMO-DAY BOARD: open items are B30 (dead create-habit button on home, day-one surface), B31 (AI stops after mic pause/resume, voice-session), B26 (advanced path skips the LLM, product scoping), B25 (plan-review card), B4-class overlap recheck, !411 rebase, !415 span-walk (needs the QA password handed to that operator). Promote parked pending Yair's QA-perfect call.
- 2026-07-04 morning (2) — B30/F1-F5 TRIAGE: Timothy's frame at the "create-habit not working" moment shows /flow-preview/home-tour on a DARK LOADING SPINNER, and his build is bracketed to MID-TRAIN (tour adapter merged late in the train), so the F1/F3/F4/F5 cluster (tour + check-ins not advancing, home create-habit) is most consistent with mid-train artifacts. Static home "Add a habit" is wired (navigates /habits). Conductor's generic completion walker is INCONCLUSIVE on the current build (its option-chip heuristic cannot drive the check-in components; all three flows render clean, zero page errors). Decisive check delegated to the builder session's own end-to-end harness against current staging; any real failure there becomes a fix MR in that territory. B30 stays open pending that result; B31 (AI stops after mic pause/resume) is the surviving genuinely-new item, needs a voice repro (round-2 voice spot-check), fix on the anchor once reproduced.
- 2026-07-04 13:4x IDT [context-lane] — C-LOOP-3 TEXT MATRIX RUN on CURRENT staging a4d90167 via gg-qa-iota (bundle-fingerprinted: staging Supabase ref + !421 marker; driver scripts/qa/context-matrix-driver.mjs, user qa-onboarding-fable, 21 turns, both fork paths, self-reset around each). Full per-beat verdicts + evidence: gg-spec docs/qa/context-matrix-2026-07.md (branch context-qa-matrix-2026-07, NOW PUSHED) mirrored with raw transcripts on context-lane-status-2026-07-03 (docs/fix-reports/c4-live-2026-07-04/). Trunk + beginner tail beats PASS. NEW LEDGER ROWS: **B32** = advanced lane has NO step ladder in systemPromptAddendum.ts (ladder is beginner-only) → advance_step(5) rejected right after submit_brain_dump → ERROR-RECOVERY walks the coach into re-planning the flow OUT LOUD ("we need to set up your habits individually", contradicting the ADVANCED beat spec); B12 stale-ladder class on the lane the rewrite never covered; fix = addendum advanced ladder (context lane) but step identities need builder's !411 derived maps — NEEDS-COORDINATION. **B33** = MAX_HABITS=2 (onboarding/schemas.ts:44, enforced handlers/addHabit.ts:82) rejects the advanced user's THIRD habit on ADVANCED-FREQUENCY; confirm_plan then completes without it = silent data loss on the lane whose premise is "bring your existing habits"; compounds B26 (brain dump persists raw text only, so add_habit is the only persist path today); NEEDS-YAIR on the intended advanced cap. **B34** = addendum SPECIAL CASE (systemPromptAddendum.ts:19) makes the coach collect days+time on BEGINNER-03 while the flow ships a separate BEGINNER-04 schedule beat — codified contradiction, observed live (coach asks schedule on the select beat); M4 collision cluster. AI-LEARNING (not new B-rows): coach recites option lists on DIRECT asks, 3/3 probes on BEG-01/02/03 despite !409 rules verified served (buildSystemPrompt.ts:119) — model disobedience under "what are my options?/read them to me"; fix = C2 rule strengthening (context lane), NEEDS-YAIR whether refusing a direct read-request is the intended UX; plus minor style breaches ("step" spoken, generic praise). VIDEO-CANDIDATE TRIAGE vs !409: evening-reflection confusion (VID-advmint-003) NOT covered (clarity-of-opener, not narration; BEG-07 opener never explains the three styles) → stands as AI Learning ai::screen, wording Sheet-owned NEEDS-YAIR, batch with W1-W5; duplicated-coach-line-before-B31-stall NOT covered (no repetition rule in !409; mechanism unknown, model-repeat vs transcript/Vapi event duplication B4-class) → held pending the C5 B31 repro, reroute to Bug if the repro shows event duplication. C5 voice spot pass + B31 pause/resume repro starting next (10-session Vapi budget, 2 B31 attempts max, console+network capture for the anchor).
- 2026-07-04 14:3x IDT [context-lane] — C5 VOICE SPOT PASS DONE + B31 REPRO ATTEMPTS. Structural finding first: NO beat on the published flow is Vapi-brained (every conversational node = fill.brain "direct-llm"), so the 10-session Vapi budget was unspendable — the QA Vapi toggle cannot start a session on this build, vapi-gate logs engine=idle(not-vapi) permanently, and M1/M2/M3 (Vapi's stale screens-era contexts, no RULE 11) have ZERO runtime impact until track 1 ships Vapi-brained beats (the vapi:sync post-merge action queues for then). Live voice today = Soniox-in → /api/llm → TEXT-ONLY reply. NEW ROW **B35 (high, demo-visible)**: dynamic LLM replies are NEVER spoken in onboarding voice mode — engineForTurn.ts:59 hardcodes speakReplies:false on the chat-native direct-LLM branch ("TEXT only (no Cartesia)", written when beats were expected Vapi-owned); user hears MP3/Cartesia openers, then every reply is a silent bubble; 4/4 sessions zero TTS traffic; useOnboardingChat's chunked-TTS reply path sits ready behind the flag (one-line-class fix, anchor territory + product call on full-duplex speaking). **B31: NOT reproduced in 2 explicit attempts** (real-flow profile beat + preview advanced-input, Timothy's location): mic paused via orb and resumed, /api/llm dispatches continue seamlessly both times. BUT session S3 captured the SYMPTOM with NO mic action (category beat): first voice turn answered, then 5 consecutive voice turns transcribed + rendered with ZERO /api/llm dispatches and zero console errors — silent turn-dispatch drop; S4 with ALTERNATING utterance texts dispatched 7/7, S1 dispatched identical repeats fine at profile → wedge correlates with consecutive identical transcripts on an option beat (root-cause candidates: transcript dedup/turn-commit guards in useOnboardingChat vs the voice-turn commit in OnboardingVoiceProvider). WORKING HYPOTHESIS for Timothy's B31: B35 (replies always silent) + the S3 dispatch drop; the pause/resume was likely coincidental. REPRO PACKAGE FOR THE ANCHOR: docs/fix-reports/c5-voice-2026-07-04/ on context-lane-status-2026-07-03 (S1–S4: per-session timeline, console.jsonl, network.jsonl, thread snapshots, screenshots, + the Playwright fake-mic harness). Also: F-C4L-1 (option-list recitation) confirmed input-mode-INDEPENDENT — the voice path recited the full 8-category list on 1/3 asks (S4). Duplicated-coach-line candidate (F3): no duplication observed in any of the 4 voice sessions; stays low-confidence AI Learning pending a Vapi-live build. Habit-schedule voice re-check deferred to post-B35/B32. Full C5 section: gg-spec docs/qa/context-matrix-2026-07.md (branch context-qa-matrix-2026-07, pushed).
- 2026-07-04 midday — ROUND-2 VIDEO 1 ANALYZED (034890a): CONFIRMED FIXED live: welcome opener, mic beat, fork discipline, orb-clearance taps, voice answers filling cards (first live voice-in proof). NEW WAVE (voice-path surfaces): B32=F5 plan-review "Let's go" does not transition into the app (DEMO-CRITICAL, onboarding finale dead-ends); B33=F2 duplicate opener text every beat with voice on (static opener bubble + live transcript echo both render; matches yesterday's "two sounds"); B34=F1 age+gender merged into one bubble ON THE VOICE PATH (text-path openerTurns fix holds; voice path bypasses it) = B5 voice-leg; B35=F4 habit-schedule card vanishes after fill on the voice path (B21-family). AI learnings F3 (line repeated 3x) + F6 (goal-vs-habit confusion) -> context lane fold-in. Bundle B32-B35 dispatched to the builder-lane session (anchor authorizes the voice surfaces; roomiest account; order: flows verification first, then B32+B33, then Yair's flow edits when they arrive, then !411, then B34/B35).
- 2026-07-04 midday (2) — LEDGER RECONCILIATION: the context lane's four bugs collided with the already-assigned B32-B35 (video wave). RENUMBERED, authoritative from here: B36 = addendum step ladder is beginner-only, advance_step rejected on the advanced lane and the coach re-plans aloud (coordinate with !411's derived ladder); B37 = MAX_HABITS=2 silently drops the advanced user's third habit (NEEDS-YAIR: intended cap?); B38 = addendum tells the coach to collect schedule on BEGINNER-03 while the flow has BEGINNER-04 for it (codified contradiction, observed live); B39 = dynamic LLM replies are NEVER SPOKEN in voice mode (engineForTurn.ts:59 hardcodes speakReplies:false on the chat-native direct-LLM branch; openers speak, every reply is a silent bubble, 4/4 sessions; DEMO-VISIBLE; anchor takes it now). B32-B35 remain the video wave (plan-review dead-end, duplicate opener text, merged profile voice-leg, schedule card vanish). ALSO from the context lane's C5: B31 re-framed: pause/resume did NOT repro (2 attempts); the real symptom captured WITHOUT mic action = five consecutive IDENTICAL voice turns produced ZERO /api/llm dispatches (silent dedup drop; alternating texts dispatched 7/7); working hypothesis: Timothy's stall = B39 silent replies + this dispatch drop; repro package docs/fix-reports/c5-voice-2026-07-04/ on context-lane-status-2026-07-03. Vapi note: no beat on the published flow is Vapi-brained (fill.brain=direct-llm everywhere), so the QA Vapi toggle is inert on this build; live voice today = Soniox + direct-LLM + TTS. THREE NEEDS-YAIR: (1) direct-ask UX: coach refuses to read option lists aloud, intended? (2) advanced MAX_HABITS cap, (3) BEGINNER-07 opener wording batch W1-W5.
- 2026-07-04 afternoon — TRUNK CUTOVER handled (see banner). ALSO: the identical-turn silent drop is FIXED (draft !428, built by the one-off session): root cause was two interacting drops, (1) routeOrbSend returned noop for any turn arriving mid-stream so the queue-and-cancel branch was unreachable (repeats die exactly when the coach is busy), (2) the utterance-aggregation pause re-armed on every interim with no ceiling so a fast repeat cadence starved the flush forever. Fix: routing no longer sees transient stream state (mid-stream turns queue + cancel the stale reply) + a 6s TURN_HOLD_MAX_MS flush clamp. Live-verified 5 identical turns -> 5 dispatches on a preview; 1618/1618 green. Paired with B39 (!425, verification pending) this fully explains the reported voice stall. Conductor merging !428 and !429 machine-gated.
- 2026-07-04 evening — !428 (identical-turn drop) and !429 (iota alias on main) MERGED; gg-qa-iota re-aliased and LIVE on the current build (gg-qh5cnc775) with the full fix stack: the team's QA surface is current again. CI ODDITY, time-boxed: every main pipeline reads FAILED while all actual jobs (verify, deploy_qa_preview) are green; no failed/canceled/external jobs exist via API; deploys and aliasing unaffected. Conductor gating henceforth uses JOB-level status on main, not the pipeline rollup. Root cause TBD for whoever next touches CI (suspect something in the sync-stage rule evaluation or a GitLab quirk on this repo's main). B25 handed off as a one-off (paste delivered). Board: !425 verification pending; builder bundle B32-B35 + !411 in flight; !426 retargeted awaiting merge; skimmer lane gated-ready.
- 2026-07-07 ~04:45 — RESISTER TRAILS IN (both walks COMPLETED to Home, but the night's two most severe findings). NEW B55 DISPATCHED, one suspected root: SETUP BEATS SAVE SILENT DEFAULTS. (1) UNATTENDED CASCADE (walk 1, 35 shots): with zero user input, path-fork through reflection-setup auto-advanced, silently defaulting a goal, a habit and a schedule; timing-linked (walk 2 with prompt turns had no cascade); hypotheses handed to the agent: default-save-on-advance + !466's evidence-arrival advance feeding each other, or late-draining queued turns. (2) REFUSAL OVERRIDE (walk 2, 27 shots): "I don't want a morning thing at all" -> coach configured the morning check-in anyway AND said so in a self-contradicting sentence; persisted to Home. Split: B55 owns the code side (no config persists without explicit user data/confirm; advancing an untouched setup beat saves NOTHING), B54 owns the prompt-consent law (refusal = no tool call; words never contradict actions). Also: 3-habit brain dump collapsed to ONE combined habit (player tagged B50; likely the MAX_HABITS cap squeezing; MORNING RULING NEEDED: intended habit cap on the ADVANCED lane, cap=2 is clearly wrong for users who already track many); day-picker showed Saturday after "Sunday saved" (corroborates B53's render facet); mojibake + missing-space copy (added to boundary item B56, now "confirmation text rendering: spaces + mojibake"); chained advance_step x8 (watch, round 2). Positive control: conversational drift/refusal handling on REAL turns was consistently good in both walks; the rot is in state/persistence under the conversation, matching the B55 framing. B54 supervisor properly re-grounded after its scout read a stale checkout (~/Developer/guided-growth-mvp sits on branch splash-intro, which ALSO retroactively explains Yair's localhost double-greeting sighting completely); supervisor confirmed on true main: no server-side preset snapping for habits exists (preset substitution is prompt-layer), goals snap via findClosestGoal (out of scope), both LLM and Vapi handler lanes need any server guard. Outstanding: cooperative trails (both walks), judge pass, then the boundary merge train !468 !471 + B53/B54/B55 as ready, then ROUND 2.
- 2026-07-07 ~04:00 — ROUND 1, FIRST TWO TRAILS IN (rambler 2 walks: beginner COMPLETED / advanced WEDGED at advanced-frequency; skipper 2 walks: BOTH COMPLETED). The live-persona + screenshot format is paying: 14+ distinct findings, cross-corroborated. DISPATCHED MID-ROUND (independent of the queued gate fixes): B53 = "every day/night" saves as the Weekday preset on submit_morning_checkin + submit_reflection_config while the recap claims daily (rambler x2, skipper x2, + "reading nightly"->Mon-Fri) PLUS the skipper facets relayed: "nine PM" saved as 9:00 AM, and the day-picker RENDERS weekdays-only even when days [0..6] saved (two layers: handler + component). B54 = capture-integrity family: free-text habit replaced by nearest preset ("phone charging in kitchen" -> "No screens after 10 PM"), fabricated Sleep habit from a clarifying question, PLUS generalized per skipper evidence: fork "pick one for me" silently invents beginner and advances, "speed this up" fabricated record_checkin(sleep=5) (nondeterministic), and the FALSE CAPTURE CONFIRMATION (coach recapped a brain dump no submit ever fired; screen showed empty input). One integrity law, three legs: never invent data, never decide required choices silently, never confirm an unacknowledged save. HELD FOR RETEST ON ROUND 2 (the !468 gate fix likely changes them): the advanced-frequency wedge (coach narrates moving to goals, beat never renders) and the chained advance_step (incl one turn targeting TWO different steps). BOUNDARY-WAVE QUEUE (small, after all trails + judge): B56 = missing-space concatenation in generated confirmations ("weekdays.Now," "there!Now," 3+ recurrences both skipper walks); B58 = beatAudioOwner backoff VOLUME (60+ warns per walk = something arms audio twice on nearly every beat; guard works but the arming source needs finding; note round 1 build already carries !470). GOOD signals recorded too: extraction quality solid, self-correction saved correctly (10k->8k), profile skip correctly refused, B50-class fabrication absent in 3 of 4 walks. Seeded-account note: qa-onboarding-<persona> accounts do not exist; players used existing seeded accounts (alejandro/yonas/fable-latency/fable-builder, all confirmed 0 pre-existing habits, no hygiene caveat). Still out: cooperative + resister trails, judge pass, visual scanner, video 2.
- 2026-07-07 ~03:15 — B50 + B52 DELIVERED, HELD FOR THE BETWEEN-ROUNDS MERGE. !468 (B50) pipeline GREEN: root cause = checkAdvanceData conflated the stored-step scale with the identity scale across shared persist step 5, so leaving habit-schedule demanded state-check data; the model, cornered, misfired add_habit into the cap ("habit limit reached") and deadlocked; fix = derived ADVANCE_GATE_OWNERS (component being LEFT per stored step, per lane) + code-owned "THE CHECK-IN IS NOT A HABIT" prompt rule; proof = new permanent CUSTOM-CHECKIN persona, 31 checks 0 fail on the branch preview (no zombie card, zero limit refusals, submit_morning_checkin saves); duplicate !469 closed. The video's ~76s in-creation limit refusal was ARITHMETICALLY LEGITIMATE (MAX_HABITS=2 beginner + the QA account carried ~4 pre-existing habits) -> QA-ACCOUNT HYGIENE follow-up: the QA fresh-restart does NOT clear old habits. !471 (B52) pipeline running: the real live bug was IntroGate never reading voiceMode (SplashIntro muted defaulted false + the gesture-started element), so the greeting played with voice off, exactly Yair's clue; fixed both points + 2 tests; stale COACH-GREETING text retired in beatContexts.ts with an ORPHANED note (entry kept same-shaped for future Sheet resync); double-fire confirmed impossible on main by three independent traces. Beats Context Sheet row 28 verified: it ALREADY held the correct splash line (contrary to the agent's note), mirror in sync. ROUND 1 EXPANDED per Yair (~03:00): each player now runs BOTH paths (4 personas x 2 lanes = 8 walks); second walk on a different account, ACCOUNT-HYGIENE tag introduced for legit cap refusals on polluted accounts. MERGE QUEUE for the round boundary: !468, !471. PARKED FOR MORNING RULINGS: (1) keep add_habit on the scheduling beats or scheduling-only; (2) QA-account hygiene fix (reset should clear habits?). MODEL ECONOMICS note for the successor: pin every agent's model explicitly; one early fix agent inherited Fable (~460k tokens) by omission; players/fixers = sonnet, mechanical checks = haiku, round-2+ persona target = haiku once the step-driver rig is frozen; encode the round loop as a Workflow script after tonight proves it.
- 2026-07-07 ~02:45 — ROUND-LOOP QA STARTED (Yair-ordered overnight protocol: LLM-played personas, per-beat screenshots, fix waves looping until a clean round; findings needing a product ruling get PARKED for morning, not guessed). !470 (B51 orb reactivity) MERGED; main green at a00b9209 = the round-1 build (carries !460 !461 !462 !464 !465 !466 !467 !470). ROUND 1 IN FLIGHT: four parallel agents each PLAYING a persona live (cooperative / rambler / resister / skipper), composing turns in character off the coach's actual on-screen replies via the QA seam, screenshotting every state change to ~/Developer/claude-work/qa-rounds/round1/<persona>/, trails + findings tagged EXPECTED(B50/B52, fixes not yet merged) vs NEW. Next: judge pass over the four screenshot trails vs render ground truth, conductor triage, fix wave, round 2 on the rebuilt trunk; loop until clean or product-ruling-blocked. Audio remains explicitly out of scope for the loop (typed path only); sound/pronunciation stays in Yair's morning walk. Morning summary owed: rounds run, findings per round with screenshots, fixes between rounds, final round verdict.
- 2026-07-07 ~02:15 — OVERNIGHT WAVE ROLLING (Yair asleep; morning summary owed). VIDEO-1 REVIEW COMMITTED (gg-spec d65c9ce, conductor-review.md): confirms B50 (limit gate fired at ~76s DURING habit creation, refusal loop = model looping across distinct turns not render dedupe, check-in save blocked end-to-end) and B51 (orb static both directions, canonical orb visible = 22:12+ build); enrichment relayed to the B50 agent (limit-count legitimacy, zombie Habit Schedule card lifecycle, max-one-refusal harness assertion). NEW-ish item "no component in check-in mode" (131s+) held PENDING: state-check rows may simply have been skipped by pre-!466 beat-jumping; visual scanner + trunk state decide whether it is still real. B51 DELIVERED: !470 open, auto-merge armed on green (1826 tests, live module proof amp 0..0.647 during playback, 0 in silence; coachAudioBus taps MP3/Cartesia elements, Vapi uses its own volume-level events, resolveOrbMic applies amp independent of ring state; honest caveat: iOS Safari WebAudio untested, code fails soft). B52 SCOPE CORRECTED by a deep trace: NO double-greeting is possible on current main (COACH-GREETING never becomes an engine node since 9bae37479; IntroGate is strict either/or); Yair's sighting was a STALE LOCAL DEV checkout (also pointed at prod DB, flagged to him). B52 agent rescoped to: retire the orphaned stale COACH-GREETING text, verify/fix whether beat-opener audio respects voice-off (engineForTurn's gate covers dynamic replies only), document the stale-checkout explanation. VIDEO 2 still not uploaded (slow connection on the tester's side); loudnorm pipeline fix committed to yair-ai-files so it processes correctly on arrival.
- 2026-07-07 ~01:30 — RENDER-COPY RULING LANDED (!467 merged at 2eb21487). Implementation = REMOVAL of the 6 registry locks (fork, beginner-01/02/03/07, advanced, plus a dead bare ONBOARD-FORK duplicate key); profile keeps its two state-aware variants; the flow document (= render = Sheet) is now the single opener source for those 6 via resolveBeatOpener fallback. CRITICAL CATCH during the build: two legacy call sites (OnboardingVoiceProvider ~L1094, useOnboardingChat L224/340) did a registry-only lookup with ?? '' and would have spoken/rendered EMPTY openers for the 6 beats after removal; new shared resolveOnboardingOpener() gives all three callers identical locked-first-then-document fallback. Evidence: 1804 tests green on the gated pipeline, COOPERATIVE persona x2 on the branch preview = identical failure set to the trunk baseline (benign chain flags only; one transient submit_goals no-fire cleared on retry, judged LLM nondeterminism). Sheet catch-up DONE: Beats Context E4 (ONBOARD-01--FORM opener) updated from the stale fallback line to the two registry variants (RAW write, verified read-back); the other 6 rows were already correct (they mirror render). Main rebuilding; next gg-qa-iota build speaks the render lines. NOTE for a content pass: the registry's ask-name profile variant contains an em dash rendered in a chat bubble ("Awesome — three quick things..."), one-char edit candidate under the founder's no-em-dash deliverable rule.
- 2026-07-07 ~01:00 — ROUND-1 HUMAN QA TRIAGED (report at 00:42 IDT on the PRE-!466 build; pipeline #1167 = 22:12 IDT, #1177 = 00:23 IDT, so the recordings almost certainly predate the B47/B48 build). Three findings: (1) "multiple voices at once on category + profile" = the double-arm class; the at-source removal shipped in !466 AFTER the recording; verdict LIKELY-FIXED-PENDING-RETEST on the current build (automation is deaf here; needs a 2-min human retest of those two beats or the video's audio signature). (2) NEW B50: after habit config, a conversational CHECK-IN setup (mood/stress metrics) got routed into habit tools and tripped the habit-count guard ("habit limit reached") — check-ins confused with habits; sits exactly in the coverage report's known conversational-checkin blind spot; fix agent dispatched (repro via seam, tool scoping per beat, the repro becomes a PERMANENT harness persona). (3) NEW B51 (from the follow-up call): the in-flow orb does not pulse with the voice. Conductor code-read confirms: canonical Orb fully supports amplitude reactivity (locked react values) and SplashIntro's greeting orb is live-wired, but OrbControls only applies USER amp gated on activeRings==='right' and NO coach/TTS level source exists in FlowVoiceControls/OnboardingChatOverlay at all — a wiring gap from the !462 migration, not a lost feature; wiring agent dispatched (coach analyser on the pooled clip + Cartesia playback, both directions into OrbMic, QA-flag debug handle as proof). VIDEO REVIEWS BLOCKED on delivery: the two new recordings are NOT in the GG QA Recordings Drive folder (newest files there 19:00 IDT); watcher cannot see Mattermost uploads; Yair told the folder is the trigger.
- 2026-07-07 night (2) — TRUNK VALIDATED + COPY RULING EXECUTED. Trunk validation: the 4-persona convo harness ran against gg-qa-iota ITSELF (main 08ae6503, the build Yair walks): B47 beat-order and B48 opener-lock PASS on all 4 personas, 57 pass / 12 fail with 12/12 fails = the benign prompt-mandated noChainedAdvance flag, zero new anomalies (report gg-spec tools/convo-harness/reports/trunk-validation-2026-07-07.md @ 0a369c4; note 3815 on !466). ROUND-2 WALK IS GO. COPY SYNC AUDIT (Haiku agent, read-only, report ~/Developer/claude-work/gg-sync-check/report.md): 12 beats checked; 5 in sync; profile beat = registry's two state-aware variants are functionally superior (keep); 6 beats DIVERGE where !466's registry-wins mechanism resurfaced OLDER registry copy over the render-iterated lines (fork, category, goals, habit-select, reflection-setup, advanced) — two of them change the ASK, not just tone (advanced: registry "tell me everything you want to achieve" vs render's skimmer ask "read me the habits you already track"; reflection: registry offers guided-vs-freewrite choice vs render's "use these three questions"). Master Sheet Beats Context is NOT stale on those 6 (it mirrors render); no Sheet write performed. YAIR RULED: render lines win on all 6, profile keeps registry variants. Fix dispatched: preferred implementation REMOVES the 6 registry locks so the seed (= render = Sheet = consolidation pipeline) is the single source via resolveBeatOpener's fallback path, profile lock kept, fallback-resurface + fork bubble+verbal structure verified, harness expectations updated, one-persona harness check on the branch preview as acceptance. Sheet needs only the profile row updated to reflect the two registry variants AFTER that MR merges. Also: QA coverage report for the night committed (gg-spec docs/qa-coverage-2026-07-07.md @ 6dbc6ae) — what each QA layer actually proves and the honest gap list (audio/visual/mobile/morning-checkin/Weekly/refresh-matrix/cold-key-mint).
- 2026-07-07 night — B47/B48 CLIENT ROOT-CAUSE FIX MERGED WITH HARNESS PROOF. !466 merged at e2727851 (pipeline green, 1800 tests); !465 (QA text-injection seam, rebased) merged at 08ae6503 — the convo harness now runs against every future build of main. WHAT !466 FIXED: (1) B47 cause 1: the consolidation seed's designer-source.json node ORDER contradicted the canonical persist-step scale (setup block 6-9 sat between profile 1 and fork 2); reordered (stepMaps regenerated byte-identical, proving the scale was canonical), lane-merge node derived from designer order (state-check), evidence-arrival advance added in useFlowOrchestrator for post-lane setup beats, resumeFromServerRow adjusted so refresh semantics survive. (2) B47 cause 2 (found live by the acceptance run, iteration 2): card-adapter voice auto-submit raced the coach-driven advance and applied stale captures to the NEXT beat (category->habit-select with goals skipped); applyAndAdvance now origin-node-scoped, cards bind captureFor(nodeId). (3) B48: flow route never consulted onboardingOpeners.ts; new renderer/resolveBeatOpener.ts resolves locked openers first with {name} substitution, all locked beats. HARNESS ACCEPTANCE: B47 beat-order and B48 opener-lock PASS on ALL 4 personas (report gg-spec tools/convo-harness/reports/b47-b48-clientfix.md @ 714d4f2; table on !466 + note 3810). Remaining persona flags are ONLY noChainedAdvance (submit_*+advance_step same turn), which the CURRENT system prompt MANDATES (SAME-TURN LAW); benign post-fix. FOLLOW-UPS QUEUED: (a) !463 stays draft — its server-side overshoot guard is optional defense-in-depth, decide slim-land or close; (b) locked-copy divergence: registry lines now visibly replace render-seed copy on goals/habit-select/reflection opener paths per the registry-wins ruling; reconciling is a content edit in onboardingOpeners.ts — flag to Yair if round-2 copy reads differently than the render; (c) mic beat's "Not now" is a styled SPAN not a button (a11y + testability); (d) state-check as lane-merge node has no back affordance (same rule as into-app). Worktrees kept: gg-b47b48-client, gg-seam-rebase.
- 2026-07-06 late night — CONVO HARNESS ONLINE + !463 REFUTED BY ITS OWN EVIDENCE GATE. The conversational QA harness (gg-spec/tools/convo-harness @ d682f546: 4 text-injection personas driven through a QA-flag-gated window.__ggQaSendUserTurn seam) ran the required before/after on !463 and FAILED it: B47 and B48 reproduce identically on !463's preview (all 4 personas; fail counts flat-to-worse). CORRECTED ROOT CAUSES (report tools/convo-harness/reports/mr463-before-after.md + note 3803 on !463): B47 is a CLIENT bug — useFlowOrchestrator.ts resolves the visible beat off the non-monotonic persist scale (V3: 1,6,7,8,9,2,3,4,5,5); server correctly returns current_step 2 but the DOM jumps to state-check BEFORE the LLM even resolves (llmCalls:0). B48's opener never comes from onboardingOpeners.ts on the chat-native route — it renders the designerToFlow.ts ~L169 hardcoded fallback. !463's server-side guard is real but one layer down; !463 RE-DRAFTED, do not merge as B47/B48 closer. ACTIONS: fix agent dispatched on the two client root causes with the harness as the acceptance gate (all 4 personas must pass B47/B48 assertions on a fix preview); seam MR !465 opened (conflicts with post-!462/!464 main; rebase folded into the fix agent's brief). KNOWN GAP logged: /flow-preview/morning-checkin has zero live LLM wiring; the real check-in conversation is only reachable via ?checkin=morning on /home with an ONBOARDED account, and all seeded QA accounts are fresh — harness morning-checkin persona blocked until a seeded onboarded account exists. Meta-lesson banked: the evidence gate did exactly its job; without the harness !463 would have merged tonight and B47/B48 would have "shipped fixed" while still broken.
- 2026-07-06 — ORB LANDED. !445 (canonical orb foundation: Orb.tsx/orbConfig/orbView, byte-identical to Yair-approved feat/orb-canonical, reconciles the older !433 home-bar copy to ONE component) MERGED at 341bd976. !446 (beats 3+5 sequences on main's welcome components, preserves adoptedOpener pipeline + onComplete + MicPermissionAdapter props, beat 5 docks mic-gold) armed to auto-merge on full green. Coordination relayed to Lane A as note 3642 on !444: hideOrb/componentOwned must suppress the FlowVoiceControls floating orb during componentOwned beats (else two orbs on beat 5); post-schema follow-up flagged: migrate OrbControls off DualButton to the canonical Orb. Orb landing session's full handoff: Yair-Context/handoffs/HANDOFF-orb-landing-conductor.md; worktree ggmvp-orb-landing left in place. CONSOLIDATION BOARD: freshness cert delivered (content sha 1741d095, captions for every clip at de67b298); Lane B unblocked on gate 2, reviewing !444; !444 fixing its red verify; parity harness lane running externally.- 2026-07-06 night — FLAG BUILD SMOKED: SAFE FOR THE ROUND, both flags PROVEN LIVE. stt-rt-v5 confirmed in the Soniox WS handshake; measured last-final-token -> /api/llm dispatch = 253ms (semantic class, vs the 900ms+ timer era). 6/6 beats advance, /api/llm 200s, ZERO console errors across three runs, zero audio-ownership violations. The gesture-opener splash AbortError demoted to a pre-existing headless autoplay quirk (stays on watch only if human videos report silent greetings). Flags stay ON for round 1. Two watch items for tonight's triage: (1) [gesture-opener] play() AbortError on splash_welcome.mp3 inside a user gesture, if tonight's videos report a silent greeting this is the lead (distinct from the fixed B43 mount race); (2) session-log screen-mapping gaps for /onboarding/qa and /onboarding/flow (telemetry, low). Cosmetic: React Query devtools panel floats on the QA build and once intercepted a click, may confuse testers, candidate one-liner to hide on QA.
- 2026-07-06 night — EVENING VIDEOS ANALYZED, one correction applied. The two 18:44/18:59 recordings post-date the fix deploys BUT fall inside the OpenAI quota-dry window (funded later that evening), so their headline "fresh" LLM-stream hard-failures are reclassified OUTAGE-EXPLAINED (the analyst had checked MR deploy times, not the quota timeline). Confirmation comes free from the team's requested retest on the funded account. REAL fresh item kept: the coach mangles the user's name in speech ("Mintesna" class, TTS/STT name handling, distinct from B44's template leak), queued as a normal finding for the name-pronunciation area (relevant: the name-capture spec exists from the founding-users work).
- 2026-07-06 evening — LATENCY + B44 ROUND CLOSED. MERGED: !459 warmer (measured: cold coach call 4113ms at 62min idle vs ~530ms warm, 7-8x; warmup endpoint + app-open ping + warmup_roundtrip_ms span) and !460 (B44 opener-fallback applyName fix + liveness verified test-pinned + ONBOARDING_LLM_MODEL env flag; second CI round after the agent's own tests initially failed in clean CI). gg-qa preview now activates gpt-4o-mini on next build per Yair's clustered-like-Vapi ruling. !457 (dark semantic turn-end flags) rebased (one union-type conflict, behavior-identical flags-off, 59/59 targeted voice suites) and armed on pipeline 1154. MEASUREMENTS APPENDED to the diagnosis doc (7037a8c): history tail grows (p90 2s at 15 turns) but p50 stable; Soniox first-partial p50 1224ms of which STT itself only ~200ms, KEY MINTING (0.5-1.5s) + WS open dominate -> pre-mint at mic-permission posted as the next big input lever (!457 note); Vapi bench needs the app's real client (5 of 6 sessions unspent). Team retest message sent (voice answers should now save; the afternoon's reports all fixed same-day). Nightly latency schedule retargeted to gg-spec main (ownership taken); latency lane's two gg-spec branches merged to main.
- 2026-07-06 evening — OPENAI OUTAGE RESOLVED + MODEL RULING. Quota exhaustion was the root trigger of the day's LLM failures (B41's 500 tail, B44's fallback exposure, the Vapi QA 429s); Yair funded, conductor verified restored with a live 1-token call. YAIR RULES: onboarding moves to gpt-4o-mini, aligned with the Vapi assistants ("clustered like Vapi", one brain everywhere). ONBOARDING_LLM_MODEL=gpt-4o-mini set on gg-qa preview scope, activates when the flag MR (B44 fallback fix + configurable model, in construction) merges and rebuilds; a comparison walk gates making it the repo default. Weekly hold RELEASED: credential patch + acceptance run may resume.
- 2026-07-06 afternoon — B42 + B43 FIXED AND MERGED (!458 at ab4c7ec6, 1756 tests green). B42: only mp3Assets[0] was preloaded per beat, every later narration clip cold-fetched at reveal time (the note-3673 prefetch gap, confirmed never closed); fix warms every clip per node; reveal gaps 1.6-3.1s (from 3-10s and a 30s cold zero-row run). B43: IntroGate re-derived hasProgress from a live cache subscription while FlowOnboarding's mount effect wrote saveStep(1) mid-greeting, flipping it and bailing out of SplashIntro; fix snapshots hasProgress once at mount; greeting now appears 3/3 fresh starts, plus a new IntroGate.test reproducing the race. WITH THIS MERGE THE BOARD'S BUILD WORK IS DONE: consolidation accepted, voice stack complete, pacing fixed, greeting reliable. Remaining: !454 weekly acceptance (resumes on the OpenAI-credential patch), the harness caption-measurement debt, and YAIR'S QA RUN, now formally unblocked.
- 2026-07-06 afternoon — PARITY WALK COMPLETE (report tools/parity-harness/reports/live-3321b820.md, note 3754 on !453). VERDICTS: (1) the post-profile stall was HARNESS NOISE, three full walks completed all 25 beats end to end; (2) CONTENT ACCEPTED: copy 19/20 PASS, verbal 5/6, the app matches the render's words; (3) TWO REAL BUGS: B42 state-check reveal pacing (rows bloom 3-10s apart, one cold run 30s empty; suspect serial clip fetch at reveal time, i.e. the note-3673 prefetch requirement possibly unimplemented) and B43 the coach-greeting overlay skipped on ALL fresh starts (known-flaky pre-seed, now consistent); fix agent dispatched for both (one MR). (4) HARNESS DEBT: blob: object URLs defeat clip-name comparison and slow reveals defeat caption capture, so captions read 18/39 without meaning; harness needs blob-resolution + patient reveal waits before the caption story is measurable; queued as a follow-up so the acceptance rerun after the B42/B43 fix produces a true caption score.
- 2026-07-06 midday — LATENCY LANE DELIVERED (addendum missions + v2 phase 1). Semantic turn-end: the direct path never consumed Soniox's end signal (text heuristic + 0.9-2.8s adaptive timer decided turn end); wiring shipped DARK behind two flags (SONIOX_V5, SEMANTIC_TURN_END 250ms fast dispatch with old timers as safety net) + a turn_end_to_dispatch_ms span; 54/54 tests. STT routing reco: Vapi cannot switch transcriber mid-call, per-user routing at call start is the pattern; Deepgram Flux (3x price) only if v5 semantic p50 > 500ms or >20% timeout fallbacks. THE 3.9s CLIENT GAP DECOMPOSED: majority = serverless cold start (LLM fn 2.5-3.1s cold vs 0.4-0.7s warm, instance dies within 1-10 min idle) + pre-timer handler work (~2.8s cold pg pooler connect) + two redundant bootstrap round trips. CONDUCTOR RULINGS: warmer fix FAST-TRACKED (no engine files, biggest perceived win); phase-2 gate is OPEN (the seed merged); SONIOX_V5-first rollout on QA approved; POSTHOG_API_KEY + project id added as gg-spec CI vars for the nightly span-regression runner. Vapi session-start benchmark still waits on Yair's QA-account credit top-up.
- 2026-07-06 midday — MIGRATION 055 APPLIED ON STAGING by Yair (verified: weekly_sessions 200 via PostgREST). Blocker notes cleared on !454/!455; the Weekly evidence chain is free to run (presets seed -> acceptance voice session -> staging evidence -> ready -> conductor merges). Also executed on Yair's sign-off: gg-qa preview env swap to the QA Vapi account (VITE_VAPI_PUBLIC_KEY, VITE_VAPI_ASSISTANT_ID, and server-side VAPI_PRIVATE_KEY, preview scope only; prod scope untouched; effective next CI deploy), and the QA weekly prompt's blank-template head strip approved (via lockfile, relayed). Parity walk running against live 3321b820.
- 2026-07-06 night — B40 + B41 FIXED AND MERGED (!456 at 3321b820). The per-beat audio-ownership registry (beatAudioOwner claim/release) is wired into all four audio paths (mp3 opener, cartesia opener, narration BeatView, the chat sendOpener speech), componentOwned auto-claims, second claimants noop + warn, 11 contract tests. B41 root cause: barge-interrupt firing on empty text; the fix gates it (if text.trim()), and post-fix evidence shows /api/llm returning 200 fetch_done where 100% aborted before. B40 evidence: zero overlap on all reached beats + the contract locked by unit tests (the original repro beat unreachable due to a walker gap on the post-seed build, noted as WATCH: the parity walk must confirm the full flow walks clean; if IT also stalls after profile, that is a real post-seed issue, not harness noise). Post-merge pipeline watched. With this merge the stable voice experience is nominally complete on iota: one voice per beat, synced captions, working coach calls.
- 2026-07-06 night — WEEKLY LANE: !451 MERGED (QA-account lockfile sync). Evidence: 20/20 tools hash-identical, convergence dry-run 0-create/0-update, live settings dump matches the turn-taking ruling exactly (v5, [en,he,es] kept, wait 0.7, noPunct 1.5, numWords 2, endpointDelay 1500, gladia fallback, speaks-first+48s). DRIFT AUDIT FULLY RESOLVED: QA and prod Coach Yair prompts now byte-identical (the "older prompt" was a stale addendum the sync fixed); the which-prompt-is-newer decision item DISSOLVES. !454/!455 remain drafts on ONE human step: migration 055 must be pasted into the STAGING Supabase SQL editor (prod has it, staging does not; the session's permission gate rightly refused DDL on shared staging; SQL loaded onto Yair's clipboard by the conductor). PENDING YAIR: (a) run 055 on staging; (b) sign-off: swap gg-qa preview VITE_VAPI_PUBLIC_KEY+ASSISTANT_ID to the QA account (completes QA isolation; conductor recommends YES, drift is eliminated so behavior parity holds); (c) cosmetic: strip the Vapi blank-template head sentence from the QA weekly prompt (recommend yes). PROD turn-taking PATCH stays gated on Yair's feel-check per the rollout plan.
- 2026-07-06 night — B40 + B41 FULLY CHARACTERIZED (instrumented repro, 4 walks, scratchpad b40-b41/events2.jsonl). B40 CONFIRMED: at the profile beat a 7.3s Cartesia blob overlapped ONBOARD-WHY-INTRO.mp3 firing mid-clip; the two arming call sites are BeatView.tsx:117 (useBeatOpenerCartesia via isCartesiaOpener) and useOnboardingChat.ts:355 fireOrDeferOpener -> sendOpener (useLLM.ts:440); only the one-sided speakReplies=false (engineForTurn.ts:59) stands between them, no cross-hook lock. B41 UPGRADED to systemic: 100% of /api/llm calls failed across all 4 walks ("signal is aborted without reason", 72ms-3092ms, zero successes, +18 server 500s); not reflection-specific, the video just caught it where the tester noticed; smells like an effect-cleanup AbortController firing on re-render churn. Both verdicts: pre-existing architecture gaps exposed by the driver era, not fresh breakage. FIX DISPATCHED as one MR (shared files): per-beat audio-ownership registry (claim/release, later claimants noop+warn, componentOwned auto-claims) + abort-lifecycle root-cause-then-fix, with post-seed retests first since the seed replaced the opener paths. Evidence harness reusable (walk2.js).
- 2026-07-06 night — THE SEED IS MERGED. !453 (the 25-beat content from the render, seeded at de67b298) merged at e5453bc1 after the two beat-context fixes landed (parity tests 16/16, onboarding suite 288/288, tsc clean) and the flagged backId seam verified (state-check back -> profile, test-pinned; correct given the why-intro merge into state-check). With !443 + !452 already in, THE CONSOLIDATION'S BUILD PHASE IS COMPLETE: main now carries the full render-designed onboarding (25 beats, both branches, women's variant, projections, custom-entry, 38 clips with per-word captions) on the hardened engine. Post-merge pipeline watched at job level. NEXT GATES: (1) the parity walk (Lane B runs it post-merge per its handoff; it is the acceptance test), (2) the B40/B41 fixes (repro in flight; B40 overlap likely intersects the new opener pipeline, RETEST both on the post-seed build since the seed replaces the opener paths involved), (3) Weekly trio evidence, (4) Yair's QA-perfect run, then promote. why-intro dead-code cleanup queued as a non-blocking chore MR.
- 2026-07-06 — SEED STACK: BOTTOM MERGED, TOP GATED. !443 (converter) and !452 (37 clips + per-word captions, retargeted) merged to main; behavior-neutral, shrink !453 to a pure content diff. !453 (the 25-beat content) BLOCKED by its own red pipeline: the beat-context parity test correctly refuses the two NEW create-your-own beats (ONBOARD-BEGINNER-02-CUSTOM / 03-CUSTOM) having no beatContexts entries; fix routed to the owning lane on the MR (copy source: render handoff section 2), parity walk re-runs at the gate once green. Parity harness env fully prepped (expectations already extracted at de67b298).
- 2026-07-06 — TODAY'S QA VIDEO ANALYZED (run Mint-QA-test-2026-07-06-at-13.42.10, findings pushed 47548b8). CONFIRMS both live bugs with beat locations: B40 voice overlap at the PROFILE beat (00:56, Cartesia + MP3 different words) and B41 LLM stall at the EVENING-REFLECTION wrap-up (06:58, tester re-prompted). 6 fresh findings (5 med, 1 low): F2 word-sync animation only on the FIRST bubble of a multi-bubble turn (distinct from B40; likely a narration-driver gap, same territory); F3 left orb half cannot control coach voice; F5 check-in card does not update / gets hidden; F6 coach references the morning check-in out of order after evening reflection (AI-Learning class, context); F7 reflection question unexplained (content, likely MOOT after the seed replaces the copy); F4 voice sounds robotic (low; the clips are deliberately native-slow per Yair's lock, revisit only if testers keep flagging). ROUTING HELD until the instrumented repro returns with the mechanical B40/B41 evidence; F2 likely joins B40's fix owner.- 2026-07-06 — TEAM QA REPORTS TWO BUGS on the fresh build (video uploading, watcher will transcribe): B40 MP3 + Cartesia speak SIMULTANEOUSLY with different words on opening messages (prime suspect: the just-merged narration driver !447 arming alongside a legacy opener path, the exact double-play class the A1 requirements warned about); B41 LLM calls failing repeatedly, tester advanced by tap. Repro agent dispatched (audio + fetch instrumentation, 8-beat walk, regression check vs the pre-Lane-A boundary c9ec03a8). Fix routing decided after repro: B40 belongs to the Lane A session if its driver is the second voice.- 2026-07-06 — PROD EMAIL CHECK CLOSED, verified by BEHAVIOR end to end. Live prod signup test: confirm-email enforced (no session, confirmation_sent_at set) AND the verification email DELIVERED to a real inbox, plus a direct Resend send also delivered ("both arrived", Yair). So: prod confirm-email ON, prod SMTP wired via Resend, branded template in place. Yair also hardened prod password policy live (min 8 + letters-and-digits, was 6 + none). Throwaway test user from the check deleted (or flagged for promote-day cleanup if deletion was skipped). Remaining email item before founding launch: the Resend plan bump (shared daily quota), already on the runbook. Release-runbook open item 5 (deliverable inbox e2e) satisfied for delivery; the confirm-link CLICK on prod stays a promote-day step.- 2026-07-06 — FOUNDING FUNNEL RULED by Yair: OAuth-first ("of course, they use their authentication"). Google/Apple sign-in is the founding-user path; email stays secondary behind the verification wall. Track 8 spec to be written from this ruling. Prod Supabase email check: Yair executing personally with conductor-provided steps. Lane B confirmed in motion: converter aligned to the landed Step-0 contract at 11:17, ~80 min after the gate-open note.- 2026-07-06 — LANE A FULLY MERGED. The engine capability set is on main: !444 Step 0 schema (c9ec03a8, the contract), !447 narration driver + close playback, !448 real projection grid + custom-entry + one-per-goal, !449 componentOwned/hideOrb seams. 287 lane tests green at tip, capabilities demoed on /flow-preview/lane-a-demo. Lane B gate-open note posted on !443; the content seed forks against the landed contract next. Ruled via Lane A relay: plan button label "Approve and start" (supersedes the earlier machine-truth "Start" ruling), profile demo props stripped from the seed.- 2026-07-06 — SECURITY + VAPI SWEEP. (1) /status ruled GATED by Yair: !450 gates /status + /onboarding-flow-preview behind QA_SCREEN_ENABLED (endpoint behind it already requireAdmin-guarded); zero new test failures vs baseline; armed to merge on green. (2) VAPI DRIFT AUDIT (conductor now holds BOTH accounts' keys; QA key at ~/.config/guided-growth/vapi-qa-private-key.txt, source StashSecret vapi-qa-private/public): REAL drift found. Coach Yair QA-vs-PROD: soniox v5 vs v4, 12 vs 15 tools, prompt 18875 vs 20649 chars, firstMessageMode/silence explicit only on PROD. Weekly: PROD assistant (ca479cf5) fully built (5 tools, full prompt); the QA account's weekly assistant is an EMPTY SHELL (0 tools, placeholder). CORRECTION issued to the Weekly activation session: ca479cf5 is PROD-account, QA env must use the QA account's own weekly assistant (lookup by name) and Phase 1 must sync it from the lockfile; ADDITION: reconcile Coach Yair on both accounts against the repo lockfile (lockfile wins; uncovered settings like STT version go to Yair as decisions). (3) Vapi cost safeguard confirmed by Yair: auto-recharge OFF on both accounts, manual top-ups only, KEEP IT THAT WAY. (4) Resend: second near-quota day (automated signups); paid bump stays a pre-launch runbook item; staging+prod share one Resend account/quota, worth splitting or bumping before founding users.- 2026-07-06 — CONSOLIDATION PHASE LAUNCHED. Plan: gg-spec docs/onboarding-consolidation-plan-2026-07-06.md (direction locked: one-time render -> builder/engine seed, then the flow builder + Master Sheet are the source of truth). All four spec rulings closed by Yair (same-screen women's variant, greeting completion signal, real per-word captions [data exists with the render session], gaps frame stays). ORB unlocked by Yair: "done, integrate as-is" -> a dedicated landing session puts components/orb/* + beats 3/5 sequences on main from feat/orb-canonical (reconciling with !433's home-bar orb). Sessions fired by Yair from the launch board: beats-context freshness audit + captions + Sheet catch-up (its freshness doc GATES the content seed), Lane A engine (Step 0 schema MR gates Lane B), Lane B content seed, orb landing, optional feedback-prep, team mechanics-only QA round today on gg-qa-iota. CONDUCTOR GATES: merge Step 0 fast when green; hold Lane B until BOTH gates; orb MRs merge after preview evidence; overnight Mint QA video analysis in flight (watcher's LLM step had failed; analyst re-running with stale-check discipline vs the 2026-07-05 merge wave). All comms via MR notes + lane STATUS pushes; Mattermost pings replace copy-paste.- 2026-07-05 night — STABLE BASE COMPLETE. Tonight's queue is fully merged into main: !442 word-by-word Cartesia sync (openers now reveal locked to real timestamps), !435 B33, !436 B34, !437 B35, !438 skimmer core, !439 live skimmer (B26 closed; canonical-key card fix included and verified), and !411 derived step maps (merged 19:11Z by its owning session with the keep-derivation rule, 1638 tests + 3-flow browser verification; my rebase agent found nothing left to do). Earlier today: B32 finale bridge (!440), Vapi cap 5 + onboarding exemption (!431), B37 lane-aware habit cap (!430), read-options on request (!432). Production remains gated behind the protected production branch; main/gg-qa-iota is the QA surface. WAITING ONLY ON HUMANS NOW: Yair's beats/flow-content feedback (spec via the Opus prep session, implementation on tomorrow's fresh Fable window), his QA-perfect ruling, the founding-funnel decision, prod Supabase confirm-email+SMTP check (+ Resend plan bump before launch, the 80%-daily-quota email today is the early proof), and the promote runbook execution on his go.- 2026-07-05 — WORD-SYNC MERGED INTO MAIN (!442, merge af70734a). The rebase agent resolved the two span conflicts honestly (cartesia_first_audio_ms re-attached at onPlaying in the new SSE path; mp3 + llm spans untouched), 1667 tests green, and the armed auto-merge fired on the rebased head (bd2f2206). Onboarding openers now reveal word-by-word locked to the real Cartesia timestamps, with the B28 autoplay guard intact and MP3-caption extensibility in place. Remaining queue per TONIGHT'S QUEUE: stack verification (B33-B35) verdict pending, skimmer canonical-key fix pending, then !411 last.
- 2026-07-05 — SUCCESSION PREPARED (outgoing conductor at 93% usage). HANDOFF-gg-conductor.md now carries a TONIGHT'S QUEUE block (the 5-step drive-to-done: !442 merge after rebase, B33-B35 bottom-up, skimmer fix re-verify gate, !411 last, then 'stable base complete' to Yair). All in-flight agent outputs land server-side (MR notes + pushed commits), so nothing is lost if this session dies mid-flight. Single-conductor rule: the incoming conductor takes over on Yair's word; this session then goes read-only and only answers questions.- 2026-07-05 — WORD-SYNC VERIFIED + SKIMMER RULING REVISED. !442 independent verification ALL PASS (note 3563): V1 real per-word sync (SSE endpoint, reveal correlated word-by-word against audio.currentTime, non-uniform gaps = true timestamps, no pop-in); V2 B28 autoplay guard holds (NotAllowedError path: text fallback reveals, no wedge); V3 dual timing source present (onsetsFromCaptions alongside onsetsForDisplayWords, registry empty today, ready for MP3 captions); V4 no regression (profile completes + advances; only the 2 known pre-existing /api/reflections/config 500s). Merge attempt hit CONFLICT with the just-merged !415 spans in the same opener files; delegated rebase running (feature-primary, spans re-attached at equivalent points: cartesia_first_audio_ms = first audible audio in the SSE path); auto-merge on green after. YAIR REVISED the skimmer ruling: the advanced-route fix (B26) IS wanted, just not buggy; since the two card bugs are one diagnosed root cause (canonical-key mismatch across parser tiers), the conductor took the fix over (delegated build on feat/skimmer-port-s1, forward-merged to !439's branch, unit tests for refine-updates-card / delete-suppresses-AI-re-add / overrides-survive; then harness re-verification before any merge). If it re-verifies clean, the FULL stack !435-!439 ships and B26 closes properly; if not, only !435-!437 ship and the skimmer parks.- 2026-07-05 — PRODUCT RULING (Yair): the live-skimmer FEATURE is OUT of this version; stable version first. Consequences: !438/!439 PARKED post-stable (their two card bugs no longer block anything); the stack's bottom three BUG FIXES (!435 B33 double opener text, !436 B34 merged age+gender bubble, !437 B35 vanishing habit card) STAY IN and ship in the stable build. Verification dispatched on !437's preview (contains exactly the three fixes, zero skimmer); merge order on pass: !435 -> retarget !436 -> !436 -> retarget !437 -> !437, then park !438/!439 with a comment. B26 (raw text fragments on the advanced path) reopens as a known limitation of the stable version since its fix rode the skimmer; acceptable per the ruling (advanced-path polish, post-stable). Remaining to a complete stable base: the three fixes + !442 word-sync (verification running) + !411 last.- 2026-07-05 — QUEUE MOVEMENT: !415 AUTO-MERGED on green (37853412; spans live, dashboard next window). !441 MERGED (f3645d2b; the migration-owner's dashboard repoint, api/_lib/gitlab.ts + .env.local.example, the same files the builder session was told not to recreate). !442 fix/opener-word-sync ARRIVED from the hardening session (Yair's top feature); independent verification dispatched (V1 sync-by-real-timestamps, V2 B28 autoplay guard, V3 dual timing source, V4 no regression); merges on pass. SKIMMER STACK STALLED: no commits since ~10:30, the two card bugs (LLM refine not updating names; deleted card resurrecting) still unfixed; nudge sent via Yair, conductor takes the fix over if the session is out of capacity. !411 merges once the stack + !442 land. Yair's flow-content feedback drops next; base will be complete when the stack + !442 are in.- 2026-07-05 — LATENCY LANE CLOSED OUT. T1 span proof verified (cartesia/mp3+pool/llm_ttft on both legs landing in /ingest); T2 baseline recorded (5 text-path runs; headline: opener TTFT ~7.1s client vs ~3.2s server = ~3.9s client-side gap; chat server TTFT tails to 7.8s, history-length suspect; mp3 pool healthy p50 182ms). beat_transition_ms accepted N/A on the text path (coach-driven trigger only). T3 PARKED by conductor: the opener playback path is being replaced by the word-sync SSE rewrite (re-baseline after; the recorded baseline becomes the 'before'), and the lane hit 91% capacity; chat-server TTFT tail is the first target when T3 reopens. Baseline doc folded into gg-spec main. !415 rebased onto main by a delegated agent (only conflict: useBeatOpenerMp3.ts success-callback, resolved observation-after-behavior; 1649 tests green; head 26d9106d) and set to AUTO-MERGE on pipeline green (server-side merge_when_pipeline_succeeds). Remaining queue after it lands: skimmer stack, caption-sync MR, !411 last.- 2026-07-05 — !426 MERGED (0e0a2d3f) after conductor evidence-check: detectAffirmation.ts + parse-habits-from-text.ts changes are COMMENT-ONLY (B32's detectAffirmation dependency untouched); parseProfileSpeech.ts deleted as dead (green tsc proves no imports). Watching the post-merge pipeline job-level.
- 2026-07-05 — B32 MERGED (!440 -> main 145e0767). Decisive re-test (note 3531, tool_rounds-keyed): T1 normal voice "let's go" advances (tool_rounds=1); T2 early-utterance race PASS twice even with speech starting before the beat's DOM existed (the wip b3d2b51e card-mount race did NOT reproduce); T3 note: in these runs the model itself called confirm_plan, so the deterministic sendUserTurn intercept (the belt for the tool_rounds:0 narration case the builder captured 11x) was present but not exercised; both paths now ship. Source branch KEPT (fix/b32-plan-review-handoff) since the wip branch stacks on it. HALF B of the hardening session: CANCELLED (T2 passed); the BeatView-level listener (wip/b32-into-app-2026-07-05) is downgraded to optional defense-in-depth, post-launch. The voice finale is DONE: a spoken "let's go" completes onboarding into the app. Remaining merge queue: skimmer stack (!435-!439, awaiting the two card-bug fixes + evidence), !426 (chore, will evidence-check and merge), !415 (parked per Yair), !411 (last), caption-sync MR (when the hardening session delivers).- 2026-07-05 — B32 TRUTH RECONCILED (builder-lane handoff + wip diff). CONDUCTOR CORRECTION: my earlier evidence agent's "confirm_plan succeeded 16x" (note 3512, the basis for holding !440) was KEYED ON REPLY PHRASING and is INVALID; the builder lane's SSE captures show tool_rounds:0 on 11 streams (the model narrates, never calls the tool). Standing method rule, all future voice-tool evidence: key on SSE tool_rounds + actual DOM/state advance, NEVER phrasing. B32 real root cause = TWO breaks: model never calls confirm_plan on ONBOARD-COMPLETE + no client consumer since PlanReviewPage deleted (579023d3). !440 (d4299c4a card listener + 74a159b8 deterministic affirmation intercept) PASSED the builder's before/after on preview gg-4e1x24kvz. THIRD piece discovered in the preserved wip b3d2b51e (branch wip/b32-into-app-2026-07-05): the card-scoped listener misses a "let's go" spoken DURING the opener (card not yet mounted, bus has no replay); the wip lifts the listener to BeatView (into-app-scoped). NOT duplicate work, a stacked race fix. Decisive re-test dispatched keyed on tool_rounds incl. the early-utterance race case (T1/T2/T3, scratch b32-retest/); verdict decides merge-as-is vs merge+follow-up. STALE-BELIEF CORRECTIONS for all lanes: the merge freeze is LIFTED (auto-deploy resolved 2026-07-05 by the gated production branch + Vercel repoint, verified via API; main no longer deploys production); the !440 re-test is the CONDUCTOR'S, not Yair's. Hardening handoff HALF B updated to conditional (only the beat-level race fix, only if T2 fails).- 2026-07-05 — FABLE REVIEW OF RECENT DECISIONS (conductor back on Fable at Yair's request). One real flaw found and FIXED in the hardening handoff (gg-spec docs/onboarding-engine-hardening-handoff-2026-07-05.md, corrected + pushed): it told the hardening session to bundle !435 (B33) with the caption-sync fix, but !435 is the BASE of the skimmer session's 5-MR stack; two owners + a modified base would have broken the stack. Corrected: hardening builds the sync fix STANDALONE off main, hands off !435, the conductor merges the stack bottom-up, combined verification after both merge. Two guards added to the sync fix: (1) preserve B28 autoplay-unlock behavior when moving openers from the MP3-blob path to SSE (regression risk), (2) the reveal driver must accept word timings from EITHER Cartesia SSE or precomputed caption files (planned verbatim MP3 openers can't use SSE). Other recent calls re-checked and stand: double-audio downgrade (evidence-based), production-branch gate (correct architecture), P0 false-alarm reversal, B32 continuation plan. Merge freeze status clarified: LIFTED; nothing currently meets the merge gate; planned order when evidence lands = skimmer stack (bottom-up) -> !440/B32 -> caption-sync.- 2026-07-05 — HARDENING SESSION RE-HOMED. The Pro-account hardening session hit its usage limit mid-audit (got tsc green, started the content-chain ownership audit; a worktree test failed on missing VITE_SUPABASE_URL env). Its B32 in-progress work is PRESERVED: worktree ~/Developer/claude-work/gg-b32-engine, branch fix/b32-plan-review-handoff (!440), commits d4299c4a + 74a159b8 + WIP b3d2b51e (uncommitted edits to BeatView/componentRegistry/adapterVoiceFill.test committed locally, NOT pushed). Full self-contained handoff written: yair-ai-files/Yair-Context/handoffs/HANDOFF-onboarding-engine-hardening.md (carries: continue B32 in that worktree; caption-sync root cause + reuse fix; B33 !435 to land with the sync; double-audio downgraded; the audit + content-change-guide; the test-env gotcha; FRUGAL block). Yair re-routing it to a fresh account whose usage resets today. Conductor still merges; drafts only.- 2026-07-05 — CAPTION-SYNC ROOT CAUSE (high confidence, caption-sync/ANALYSIS.md returned inline). Onboarding beat openers were NEVER wired to real per-word timestamps: speakOpener.ts computes reveal from audio.currentTime/audio.duration (start/end anchored, drifts mid-line) and falls to a flat ~310ms/word timer when Chrome reports duration=Infinity (common for streamed MP3). openerReveal.ts does Math.round(progress*wordCount), linear interpolation, not word boundaries. The REAL machinery EXISTS and works but only on the post-onboarding CHECK-IN CHAT: api/cartesia-tts-sse.ts requests add_timestamps:true and src/lib/services/cartesiaVoice.ts schedules real per-word onsets; onboarding uses the plain api/cartesia-tts.ts (MP3 blob, no timestamps). A 2026-06-25 commit downgraded the live DIALOGUE-turn reveal word->sentence when TTS moved ws->http; a 2026-07-02 commit rebuilt real word-sync via SSE but scoped to check-in only. The opener path predates all that (never timestamp-wired). CORRECTION: startAt=profile does NOT change the reveal (pure state seek), so Yair's deep-link view IS representative; the real-walk retest is moot. FIX (clean reuse, not rebuild): point useBeatOpenerCartesia/speakOpener at cartesia-tts-sse (add_timestamps) and reuse cartesiaVoice.ts word-onset scheduling instead of openerReveal's progress*wordCount. Double-TTS confirmed NOT a blocker here (sendOpener is a Vapi-path LLM-turn call, not TTS; on Vapi-off/profile speakOpener fires once). This + B33 text dedup fold into the onboarding-engine hardening Fable session as the opener-correctness bundle.- 2026-07-05 — DOUBLE-AUDIO DOWNGRADED. Yair retested in a SINGLE tab: no audible doubling. So the earlier "two MP3s + language mess" was almost certainly his multiple open onboarding tabs, not the app. The code pattern the agent found (useBeatOpenerCartesia.speakOpener + useOnboardingChat.sendOpener both able to fire opener TTS) is now LATENT, not a confirmed live bug (neither human single-tab nor harness reproduced audible overlap). Reclassified: clean up only if the hardening session is already in that code; NOT a priority chase. Confirmed opener issues remaining: (1) B33 double TEXT (human-seen in the profile screenshot; fix !435 built), (2) word-by-word caption NOT audio-synced (Yair's main concern; investigation in flight). B32 into-app bridge still separate.- 2026-07-05 — WORD-BY-WORD CAPTION SYNC flagged by Yair (one of the main features). Code check: beat openers (ProfileBeat.tsx) use a CLIENT TIMER reveal (shownWords ticks up, coachText.split(' ')), NOT locked to the Cartesia audio; the SPLASH does use true pre-computed word timestamps (splashCaptions.ts via mlx-whisper); a Cartesia realtime WS path exists (useOnboardingRealtimeSync.ts). So beat-opener 'karaoke' is a timer approximation, not true audio-sync. Confounds: Yair viewed it on the startAt=profile QA deep-link (may render static, not representative) and the double-TTS bug would wreck any sync anyway. Dispatched a read-only investigation (scratch caption-sync/ANALYSIS.md) to determine: timer vs timestamp vs WS on beat openers, whether /api/cartesia-tts requests+consumes add_timestamps, regression check, startAt effect, and the minimal path to true word-by-word sync. This + the double-audio fix + B32 belong together in the onboarding-engine hardening Fable session (all 'the opener speaks/reveals wrong').- 2026-07-05 — OPENER BUGS SPLIT INTO TWO (B33 verification, note 3526 on !435). TEXT (!435): the hideOpener fix is mechanically sound and ships a regression test, but the headless harness could not trigger the live-LLM-stream opener race (QA shortcuts + fake-mic miss the real liveOpener/deferred-appendMessage window), so it is a code-confidence + human-repro merge (Yair's screenshot is the 'before'), not an empirical before/after. AUDIO (separate, CONFIRMED via code): useBeatOpenerCartesia.speakOpener() AND useOnboardingChat.sendOpener() BOTH fire opener TTS on the same beats -> two parallel /api/cartesia-tts calls; this is what Yair heard (overlap + language mismatch), out of scope of !435, needs its own fix. DECISION: HOLD !435 and BUNDLE the text fix with a new double-audio fix so the opener ships fully clean in one merge (no half-fix). The double-audio fix is folded into the onboarding-engine hardening Fable session scope alongside the B32 into-app bridge (both are 'the engine speaks/advances wrong'). Nothing merged this turn.- 2026-07-05 — P0 CONFIRMED DEAD by human tap (Yair: age+gender selected fine, advances). Onboarding profile beat works for real users. NEW from the same walk: Yair saw the profile question render TWICE (scripted "How old are you?/And your gender?" + generated "Hey Yair, welcome! How old are you, and what's your gender?") and heard TWO overlapping audios with a language mismatch. The double TEXT is B33 (duplicate opener with voice on), live on main, fix built as !435 (unmerged). The double AUDIO is being characterized (could be the audio side of B33, or Yair's several open onboarding tabs auto-playing). Dispatched a verification agent to compare main vs the !435 preview with injected audio instrumentation (count play() calls + detect overlapping playback per beat), to confirm !435 dedups the text AND whether it also fixes the audio or a SEPARATE audio-doubling bug remains. Yair also doing a single-tab listen to rule out the multi-tab artifact.- 2026-07-05 — P0 PROFILE WEDGE = FALSE ALARM (root-cause investigation, high confidence). It is an OUTDATED TEST WALKER, not a product bug: /tmp/gg-release-e2e/task2-onboarding.mjs:184-220 assumes the old 3-screen profile (separate nickname/age/gender), but the current app is ONE combined age+gender card gated on valid=!!(age&&gender); the walker never selects an age, so Continue never enables, deterministically, every retry. Real users who scroll to an age + pick a gender are fine. My earlier "shared July-4 beat-advance regression / central blocker" hypothesis is WITHDRAWN: useFlowOrchestrator capture()/applyAndAdvance() is unchanged since pre-2026-07-03, and every 2026-07-04 merge-set commit (orb, read-options, habit-cap, identical-turn, B39) was full-diff-read and touches none of the tap-advance files. A5 (habits-on-home) is UNBLOCKED (a fresh account can complete onboarding). B32/into-app is SEPARATE and REAL but PRE-EXISTING since 2026-06-29 (commit 579023d3 deleted PlanReviewPage.tsx, which bridged confirm_plan->complete(); the new IntoAppAdapter never got a confirm_plan listener). Not a regression. !440 is the right fix DIRECTION (add the listener). OPEN TENSION to resolve: the B32 evidence run says !440's listener fires onCapture yet still does not advance, so we must confirm whether the into-app 'Let's go' TAP advances on the current build (tap works => finale completable, B32 is voice-only polish; tap fails too => into-app completion needs more than the listener). MERGE FREEZE fully liftable now (production gated + P0 false alarm); remaining real items: the skimmer's 2 verified bugs (!438/!439) and the B32 tap-disambiguation (!440).- 2026-07-05 — B32 EVIDENCE = !440 FIX NON-FUNCTIONAL + LIKELY SHARED ROOT CAUSE WITH THE P0 (note 3512 on !440). The B32 listener is verifiably deployed and confirm_plan succeeds 16x/100s (onCapture fires), but the flow NEVER leaves the into-app finale beat. This is the SAME fingerprint as the profile-beat P0: capture succeeds, beat-advance/completion never fires. Two different beats (profile + into-app terminal), identical failure => strong hypothesis: ONE regression in the SHARED beat-advance/completion path (onCapture -> step advance in useFlowOrchestrator), introduced in the 2026-07-04 merge set, not two beat-specific bugs. This is now THE central release blocker: onboarding may not be completable on the current build at multiple beats. HOLD !440 (its fix is correct but the underlying advance path is broken, so it can't demonstrate advance). Profile-wedge investigation (agent) redirected to the shared advance mechanism with this second data point. Human phone check (profile beat) still valuable; if reachable, also check whether the into-app 'let's go' TAP advances (isolates voice-specific vs whole-advance-path).- 2026-07-05 — RELEASE-PREP HANDOFF 2 (supersedes the earlier auto-deploy + P0 pastes). KEY NEW ITEM = P0 CANDIDATE, profile-beat wedge: fresh-account tap-path onboarding wedges at the age/gender picker. PUT /api/onboarding returns 200 and age+gender persist, BUT onboarding_states.current_step stays 1, Continue disables, picker never collapses, next beat's opener renders below it. Walker used JS-dispatch clicks (bypass hit-testing) so NOT a B27 overlay artifact; server-truth current_step=1 says step-advance logic failed. Passed QA rounds 2+3 on 2026-07-03, so suspected regression from the 2026-07-04 merge set (voice-fix stack / orb !433 / read-options !432 / habit-cap !430). ACTION: read-only Sonnet root-cause investigation dispatched (scratch profile-wedge/ANALYSIS.md); human phone check on gg-qa-iota still recommended to confirm artifact-vs-real. Blocks A5 (habits-on-home) and the release. AUTO-DEPLOY (their finding 1): RESOLVED structurally by the production-branch gate (created+protected production@f8572463, mirror synced); this matches their recommended remedy (a) staged production surface. Pending: Yair repoints Vercel production branch main->production to activate; MERGE FREEZE HOLDS until then. Other rows: B30 (dead create-habit button) FIXED on this build (close its issue if open); real-signup e2e CLOSED on staging (verification email delivered to a real inbox, first sign-in ok, re-proves R2/!417 on a real fresh signup); /onboarding-flow-preview ungated in the prod bundle (small gating MR or deliberate accept; mitigated once prod is gated); day-one 9/10 (A5 blocked by the P0). Real-pattern confirmed staging account now exists: yair+release-e2e@guidedgrowthos.com.- 2026-07-05 — RELEASE-PREP DELIVERED + MERGE FREEZE. Read release-runbook-2026-07-05.md + release-signup-dayone-evidence-2026-07-05.md (gg-spec main). VERIFIED LIVE: production auto-deploys (GitHub push mirror id=1 enabled, all branches; Vercel prod branch=main; 5 prod deploys in ~13h, current f8572463). Every merge to main is currently a de-facto production release, so the "promote parked" rule is NOT holding. => MERGE QUEUE FROZEN until Yair rules on auto-deploy (pause/gate vs adopt-continuous). Nothing else was going to merge anyway (skimmer held on 2 bugs, !440 evidence pending, !411 last). Three NEEDS-YAIR: (1) auto-deploy pause vs adopt [top; I execute the mirror pause on his word, reversible]; (2) F1 founding funnel: email signup dead-ends at a verification wall enforced in authStore.ts:344-360 (can't be bypassed by disabling Supabase confirm; product REQUIRES confirm-email ON + Resend SMTP), only OAuth passes -> recommend OAuth-first, and prod Supabase confirm-email+SMTP must be configured regardless (I can't verify prod Supabase from here); (3) F2 age-wheel needs a human tap-through, line added to Yair's QA run. ALSO latent, go-time (not now, no prod audience yet): prod scope still has VITE_QA_PASSWORD + VITE_QA_SCREEN_ENABLED (remove before launch), /onboarding-flow-preview ungated on prod writing to prod DB. A5 (home reflects chosen habit) VERIFIED positive.
- 2026-07-05 — SKIMMER EVIDENCE = HOLD (!438/!439 NOT merged). Signed-in evidence pass (conductor agent, preview gg-12h1pc8mw) confirmed the good parts (instant regex cards <1s, signed-in LLM refine call 200, polarity edit survives, in-session replay shows real cards) but found TWO real bugs, notes on !439 (3498) + !438 (3502): (1) LLM refine does NOT visually update names, 2/3 stubs never refine (key-mismatch in BrainDumpCapture.tsx addParsed supersede logic); (2) a deleted card REAPPEARS under the AI's differently-normalized name (deleted-set keyed differently than the AI result). Both smell like ONE root cause: regex tier and LLM tier normalize the map key differently. Routed to the skimmer session to fix (canonical key across both tiers + deleted-set matches on it). Hard-refresh persistence UNVERIFIED (blocked by startAt fast-forward, not a demonstrated bug). B33/B34/B35 (!435/!436/!437) NOT verified this pass, evidence still owed before the bottom of the stack can merge. Net: the whole stack stays draft; nothing merged. main unaffected (everything already merged is clean). !440 (B32) evidence agent still running.
- 2026-07-05 — CONDUCTOR RECLAIMED by the original (Fable/$200) session on Yair's direction ("let's get everything going, I don't want to wait"). The Pro-account conductor is out of usage until reset; if it wakes, it must re-read this ledger and STAND DOWN until Yair re-hands-over. State observed on reclaim: !425 B39, !429 weekly day-rec, !433 orb install all MERGED overnight (main 7dc0e20b green). B32 ARBITRATION (recorded earlier in the handoff, now ledgered): duplicate fixes !434 vs !440 -> !440 wins (success-gated, readOnly CTA hidden, richer tests); !434 to close as superseded; B33-B35 ship via the skimmer stack !435-!437; the skimmer feature (!438/!439, closes B26) rides on top; builder lane stood down from B33-B35. B37 CONFIRMED by Yair ("b37 confirmed") -> !430 cleared. Now merging: !431 (ready+green), then !430. Remaining gates: !432 wants Yair's read-options confirm; stack wants signed-in evidence on !438/!439; !440 wants its voice-leg walk; !426 awaits owner-ready; !411 merges last; !415 wants span evidence (deprioritized by Yair).
- 2026-07-04 late night — CONDUCTOR RETIRED (old session, Fable/$200 account). This is the outgoing conductor's FINAL write action; it is read-only from here. The NEW conductor (VS Code session on the second account) takes the queue per HANDOFF-gg-conductor.md and posts its handover entry below this one. State at handover: main 60382461 (#966 fully green, gg-qa-iota serving it); !425 draft green awaiting voice verification; !426 draft green awaiting ready; builder bundle B32-B35 not yet MR'd; !415 span walk pending; !422 parked on scope ruling; staging locked (push/merge No one); release chain parked until the QA-perfect call.
- 2026-07-04 23:25 IDT — NEW CONDUCTOR ONLINE (Fable session, new account, VS Code). Single-conductor rule satisfied: Yair confirmed the old conductor retired (its RETIRED entry is the row above). Takeover verification complete: token `conductor` (keychain `gitlab-conductor`; scopes api + read/write_repository, expires 2026-08-03) resolves guidedgrowth-group/guided-growth-mvp by URL-encoded path (id=9, default branch main); main tip 60382461 with pipeline #966 green at JOB level (verify + deploy_qa_preview), matching the RETIRED snapshot exactly; 30 open MRs, no builder-bundle MRs yet. Queue posture adopted as-is: !425 (B39) merges when the context lane's fake-mic voice verification lands as evidence; !426 when marked ready; builder bundle B32-B35 on ready+evidence (its merge unlocks the skimmer lane); !411 stays parked until rebased onto the landed chain, merges last; !415 awaits its span walk; !422 parked on Yair's scope ruling; release chain parked until Yair's QA-perfect call. Gating unchanged: SHA-pinned JOB-level greens, evidence rules, drafts only on ready+evidence. This conductor runs on a tight-limit plan: gate checks inline, batch QA/verification delegated to subagents behind the context firewall.
- 2026-07-04 night (4) — CONDUCTOR HANDOVER PREPARED. Pipeline #966 on main fully green (60382461); gg-qa-iota serves this build. Handoff written: yair-ai-files/Yair-Context/handoffs/HANDOFF-gg-conductor.md (role, new-server infra, gating protocol, queue, single-conductor rule). The incoming conductor (new account) posts its own handover entry here on takeover; the OUTGOING conductor does no further write actions after Yair confirms the switch.
- 2026-07-04 night (3) — PIPELINE #964 EMAIL EXPLAINED (harmless) + STAGING LOCKED. A session pushed one commit (The Weekly vapi lockfile) to the retired staging branch at 22:30 out of pre-cutover habit, then self-corrected: deleted staging and re-pushed the identical content rebased onto main tip as chore/vapi-lock-weekly (verified byte-identical). The already-running pipeline's deploy_qa_preview job lost its ref mid-run (fatal: couldn't find remote ref, exit 128) — that's the failure email; verify job had already passed; NO CI problem, nothing to fix. Hole closed: `staging` is now a protected branch on the new server with push+merge = No one; recreation test push was rejected pre-receive. The owning session opened + merged its own MR for chore/vapi-lock-weekly -> main before the conductor's tracking MR landed: it is NEW-SERVER !428 (the first RECYCLED iid — old-server !428 was the identical-turn fix, merge 090b745d). Branch pipeline #965 green; merge pipeline #966 on main gated as usual.
- 2026-07-04 night (2) — NEW-SERVER SWEEP COMPLETE, conducting fully restored. Infra came up provisioned: instance runner `oci-arm-shared` ONLINE (pipeline #959 SUCCESS on main tip ecb9afa0 at 18:50Z — note: no cosmetic-failed rollup on the new server), CI variables present at project level (MATTERMOST_WEBHOOK_URL, VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID), webhooks live (self-hosted Mattermost plugin at mm.guidedgrowthapp.com + App Store Connect). Cleanup executed: stale `staging` branch deleted on the new server after verifying fully merged into main (the migration DB snapshot predated the cutover; git data was current but unpruned); 7 MRs retargeted staging->main (!366 !387 !411 !415 !422 !425 !426); !427 closed as superseded (its content is in main via merge 090b745d). IID WARNING: the old server's !428/!429 MR RECORDS did not migrate (their merge commits are in main); the new server WILL REUSE iids 428/429 for future MRs — any ledger reference to !428/!429 dated 2026-07-04 or earlier means the OLD server's MRs (identical-turn fix = merge 090b745d; ci-alias-main fix = the .gitlab-ci.yml alias commit), not whatever new MRs later carry those numbers.
- 2026-07-04 night — MIGRATION EXECUTED: gitlab.guidedgrowthapp.com is LIVE and the fleet works there from now on; gitlab.com is read-only/archived (pushes rejected). Conductor state: all local checkouts/worktrees repointed; INTEGRITY VERIFIED (new-server main == pre-switch tip on both guided-growth-mvp `ecb9afa0` incl. !428/!429, and gg-spec `265ecc5b`); all in-flight branches present (fix/b39-speak-replies, chore/parser-queue-x1-x4, fix/ci-alias-main, ...). Numeric project IDs CHANGED (guided-growth-mvp=9); use URL-encoded paths in API calls, never old IDs. Vercel path unchanged (new server push-mirrors to GitHub; never push GitHub directly). The no-new-MRs interim rule is LIFTED: open MRs/issues on the NEW server normally. STILL OPEN: conductor token lacks `api` scope (repo r/w only; API conducting waits on a rescoped token); CI runners + CI variables (VERCEL_TOKEN etc.) on the new instance unverified until first pipeline; project/group webhooks (Mattermost ping) did not migrate, re-create on the new server.
- 2026-07-04 evening (2) — GITLAB MIGRATION ANNOUNCED: moving to self-hosted gitlab.guidedgrowthapp.com (the trunk cutover was part of this). UNTIL YAIR CONFIRMS THE SWITCH: push branches to gitlab.com as normal; DO NOT create new MRs/issues/comments on gitlab.com (they will not migrate). New findings -> this ledger; finished fixes -> branch + fix report in the commit message + STATUS row; the conductor merges completed work directly (machine-gated, evidence rules unchanged). Existing open MRs continue and their records reconcile post-switch. SWITCH-DAY CHECKLIST (conductor executes on confirm): new PAT on the new instance; remote set-url across all checkouts/worktrees; CI variables re-created (VERCEL_TOKEN etc.); Mattermost webhook re-pointed; CI RUNNERS for the new instance (the Oracle box is the natural home).
- 2026-07-05 01:27 IDT [b39-verify, one-off session]: B39 FAKE-MIC VOICE VERIFICATION LANDED + PASSED; the 23:25 queue item for !425 is closed. State note: !425 is already MERGED (merge commit a5013d9c into main; current main 43b9eacb contains it; the only commits on top are the weekly-day-timezone feature, which do not touch src/lib/orb/engineForTurn.ts, so the shipped trunk carries the exact fix). Method: signed-in QA user on staging Supabase, headless Chromium 375x812 with a REAL fake mic (use-fake-device + use-fake-ui + use-file-for-fake-audio-capture playing a spoken sentence), hit-tested walk to the profile beat (fill.brain=direct-llm, Soniox voice-in live: /api/soniox-temp-key + transcribe-websocket confirmed). The fake audio drives a genuine Soniox turn so the coach produces a real dynamic LLM reply, which is exactly the speak(content) path B39 touches. Result, run on gg-qa-iota (= main): VOICE ON (voiceMode=voice) the dynamic reply is SPOKEN, POST /api/cartesia-tts fired 4x right after the turn (22.3/22.4/24.7/28.1s absolute; those are 4 chunks of ONE streaming reply, NOT one slow call; per-chunk synth 0.6 to 1.6s, first spoken chunk ~0.6s after the reply text was ready, 4 TTS audio blobs played to ok) and text rendered; VOICE OFF (toggled the voice orb to screen mode, mic left on) the SAME turn still transcribes and still renders the reply as TEXT, with ZERO /api/cartesia-tts and no blob audio. Only variable = voiceMode -> speakReplies, precisely the changed line. Unit tests src/lib/orb/__tests__/engineForTurn.test.ts 11/11 (incl. the 3 B39 cases). Evidence comment posted on !425; raw probe data in /tmp/gg-verify/B39-voice-on.json + B39-voice-off.json. Minor, NOT B39: the beat OPENER TTS took 2.1s (one batch /api/cartesia-tts, likely cold first hit) worth an eye later; the dynamic-reply path is fast.
- 2026-07-05 late evening — INCOMING CONDUCTOR ONLINE (Pro-window session, Yair-authorized takeover from the Fable conductor at 93% usage; that session is retired from writes per the succession row). Worklist = TONIGHT'S QUEUE in HANDOFF-gg-conductor.md: (1) !442 word-sync merge after its rebase lands green, (2) B33-B35 stack bottom-up on the verification verdict (!435 -> !436 -> !437, retarget dance), (3) skimmer fix re-verify gate on !438/!439 (clean = merge on stack, not clean = park, gate not lowered), (4) !411 last, (5) stable-base call to Yair + his three open items. Lean rules in force: inline checklist, no fan-outs, only the skimmer re-verify may delegate. Evidence rules unchanged (DOM/state + tool_rounds ground truth, SHA-pinned JOB-level post-merge checks).

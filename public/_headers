# Cloudflare Pages caching for the flow builder.
# Hashed assets never change, so cache them hard. The HTML entry must always
# revalidate so a new deploy shows up on a normal reload (no hard refresh needed).
/assets/*
  Cache-Control: public, max-age=31536000, immutable

/*
  Cache-Control: no-cache
